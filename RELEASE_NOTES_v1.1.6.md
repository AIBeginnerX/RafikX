# RafikX v1.1.6 — 에이전트 강화: 검증 인프라·스펙 동결·계획 오케스트레이션·모델 보정

감사·강화 지시서(docs/agent-upgrade/) 전체를 수행한 대규모 기능 릴리스입니다. 핵심 목표 — **완료 판정을 모델이 아니라 시스템이 내린다** — 가 6개 마일스톤으로 구현됐습니다.

## 거짓 완료 차단 (M1)

- **태스크 스키마와 봉인된 상태 머신** — 태스크는 `verification` 명령 목록을 데이터로 갖고, `DONE` 전이는 시스템이 명령을 직접 실행해 exit code 를 확인한 경우에만 일어난다. `CmdResults`·`VerifierVerdict` 생성자가 모듈 내부에만 존재해 모델 출력이 상태를 바꿀 수 없다(가시성 봉인).
- **증거 원장** — `rafikx verify-task` 가 명령·exit code·diff·시간을 태스크 파일과 LEDGER.jsonl 에 기록.
- **테스트 무결성 가드** — `#[ignore]` 추가·테스트 함수 삭제·어서션 순감소를 diff 에서 자동 감지해 차단.
- 레드팀 시나리오 4종(무수정 완료·가짜 통과 로그·ignore·테스트 삭제) 실제 실행으로 차단 증명.

## 검증 기본 상향·계획 데이터화 (M2)

- 검증 정책 기본값 Inherit→**Auto** — 코드 변경이 있으면 항상 검증. tests/ 가 있으면 `cargo test --quiet` 까지.
- **AC 커버리지 매트릭스** — `rafikx verify-plan`: 미매핑 AC·검증 불가 태스크 감지, 확정 거부.
- **테스트 수 래칫** — 통과 테스트 수가 과거 최고점에서 후퇴하면 완료 차단.
- 판정 불능(AcceptUnknown) 폐지 — 검증자가 판정하지 못하면 통과가 아니라 실패.

## 스펙 동결·인수 테스트 불변 (M3)

- `rafikx spec-freeze` — AC 검증 방법 필수·가정 기록 필수 검증 후 동결, 동결 후 덮어쓰기 거부.
- **tests/acceptance/ 쓰기 차단** — 파일 도구 전체(edit/write/multi_edit/apply_patch)에서 경로 거부, 읽기는 자유.
- 시스템 프롬프트 [스펙 우위] 섹션 — 질문 ≤5 한 번에·'가정' 명시·동결 기준 임의 변경 금지.

## 오케스트레이션·재개·롤백 (M4·M6)

- `rafikx run-plan` — **Executor 는 서브프로세스로 태스크 지시만 받는다**(컨텍스트 격리의 아키텍처 강제). 검증 통과 시 git 체크포인트 커밋, 실패 시 재시도→에스컬레이션·의존성 안전 중단.
- **재개** — 완료 태스크 건너뛰고 미완료부터. 재개 직전 체크포인트 재검증으로 상태-코드 불일치 감지(실측).
- `rafikx plan-rollback` — 체크포인트를 git revert 로 되돌리고(이력 보존) 태스크 PENDING. 충돌 시 트리 자동 복구.
- `rafikx plan-report` — 원장 집계 리포트. 워크 PROGRESS.md 자동 갱신.

## 모델 보정·2차원 하네스 (M5)

- `rafikx calibrate` — 9 프로브(지시 준수·구조화 출력·미니 코딩)로 능력 점수 측정. 실측 MiniMax-M3 0.44.
- 능력 점수가 낮으면 검증 정책 자동 상향(<0.5 Strict·<0.8 Auto, 하향 없음) — "난이도 × 모델 능력" 매트릭스.
- diff 크기 제한(초과 시 분해 강제), 교훈 근거 검증(관측 오류와 무관한 일반론 저장 스킵), 요약 검증 ✓ 구분.

## 내부 프롬프트 강화

- 시스템 프롬프트 신설 섹션: **[증거 우위]** — exit code 없는 완료 주장 금지·테스트 약화 금지·무변경 완료 금지. **[스펙 우위]** — 모호함은 실행 전에.
- 검증자 프롬프트 판정 전 필수 검사 4종 — 테스트 무결성·하드코딩 탐지·AC 커버리지·자기 보고 배제.

## 검증

- 테스트 **379개 전부 통과** (v1.1.5 대비 +11) — LSP 실서버 통합 테스트 포함
- 실측: 레드팀 4 시나리오 차단·래칫 후퇴 감지·체크포인트 불일치 감지·롤백·재실행 복구·보정 0.44
- 감사 문서 6종(docs/agent-upgrade/01~06) — 모든 평가에 파일:라인 근거

## 업그레이드

기존 설치는 `rafikx update` 로 최신 태그를 따라갑니다.
