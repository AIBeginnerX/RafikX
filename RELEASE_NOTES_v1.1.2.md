# RafikX v1.1.2 — 패치: 모델 카탈로그 자동 갱신

"provider를 등록하면 모델을 검색하고, 신규 모델이 나오면 주기적으로 따라간다"는 설계가 실제로는 동작하지 않던 문제를 근본 원인 4건으로 추적해 수정한 패치 릴리스입니다.

## 새 기능

- **모델 카탈로그 하루 1회 자동 갱신** — 연결된 모든 서비스의 모델 목록을 백그라운드에서 하루에 한 번 다시 받아옵니다. 마지막 갱신 시각을 `catalogs_meta.json`에 기록해 24시간이 지나면 갱신하며, CLI 시작·데스크탑 부트·텔레그램 데몬(6시간 주기 루프)에서 동작합니다. 부트를 절대 막지 않고, 실패는 로그에만 조용히 남습니다.

## 수정 내용

- **CLI 등록 시 모델 캐시 저장** — 기존엔 TUI·데스크탑의 "키 붙여넣기" 경로만 모델 목록을 캐시에 저장했습니다. `rafikx login` 연결 직후, `rafikx models <provider>`(이제 조회가 곧 갱신), model 위저드도 같은 통로로 저장합니다.
- **openai(Codex OAuth) 모델 조회 400 수정** — codex models 엔드포인트 호출에 `client_version` 쿼리가 빠져 항상 HTTP 400이던 것을 수정했습니다. 수정 후 실제 모델 목록이 수집됩니다.
- **gemini OAuth 정적 카탈로그 폴백** — OAuth 토큰은 네이티브 models API 스코프가 없어(실측 403) 매번 "실패"로 표시됐습니다. 이제 정적 카탈로그로 깔끔히 대체하고, API 키 사용자는 `x-goog-api-key` 헤더로 네이티브 폴백이 동작합니다.
- **404 시 안내 개선** — `/models`가 없는 서비스는 `models_url` 설정으로 목록 위치를 지정할 수 있음을 오류 메시지에 안내합니다.
- **카탈로그 저장 경합 해소** — 여러 프로바이더 갱신이 catalogs.json을 동시에 쓰며 항목이 유실될 수 있던 경합을 조회 병렬·저장 순차로 해소했습니다.

## 검증

- 테스트 **351개 전부 통과** (v1.1.1 대비 +3: 스탬프/만료 판단·프로바이더별 스탬프·codex URL)
- 실환경 검증: openai 9개·anthropic 10개 모델 수집·캐시 확인, gemini 정적 폴백 메시지 확인, catalogs_meta.json 스탬프 생성 확인
- CLI·데스크탑 컴파일 성공

## 업그레이드

기존 설치는 `rafikx update` 로 최신 태그를 따라갑니다.
