# RafikX v1.1.1 — 패치: 모델 신뢰성 3종

v1.1.0 바로 다음 날 나온 실사용 디버깅을 반영한 패치 릴리스입니다.

## 수정 내용

- **gemini 모델 목록 폴 fallback** — Google의 OpenAI 호환 경로(`/v1beta/openai/models`)가 제한적이라 `/model refresh`가 gemini에서 HTTP 400으로 실패하던 것을, 네이티브 `/v1beta/models` 자동 폴 fallback으로 해결했습니다. 남은 실패는 자격 증명 범위 문제로 정직하게 보고됩니다.
- **`[providers.*] models_url` 설정 추가** — `/models` 엔드포인트가 없는 프로바이더(예: commandcode, 목록 조회 시 HTTP 404)의 모델 목록 URL을 직접 지정할 수 있습니다.
- **응답 모델 불일치 경고** — 프로바이더가 요청한 모델과 다른 모델로 응답하면 "요청한 모델 'X' 대신 다른 모델 'Y' 이(가) 응답했습니다" 경고를 표시합니다. OpenAI 스트림·비스트리밍·Codex·Anthropic 전 경로에서 응답 모델을 캡처하고, 날짜 접미 같은 변형 id는 오탐 없이 허용합니다. 조용한 모델 대체가 더 이상 묻히지 않습니다.

## 검증

- 테스트 **348개 전부 통과** (v1.1.0 대비 +3)
- 실환경 검증: refresh 9곳 조회 정상(실패 2곳 정확 보고), `/model k2` 검색 정상, 비활성 프로바이더가 전 바인딩 경로에서 거부됨 확인, 변형 모델 오탐 0
- CLI·데스크탑 릴리즈 컴파일 성공

## 업그레이드

기존 설치는 `rafikx update` 로 최신 태그를 따라갑니다.
