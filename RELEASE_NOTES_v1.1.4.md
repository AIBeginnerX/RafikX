# RafikX v1.1.4 — 기능: IDE 기능 1차 + 시작 화면 리디자인

터미널 에이전트에 IDE 성 기능을 얹고, 첫 화면을 공식 로고로 완전히 바꾼 기능 릴리스입니다.

## IDE 기능 1차

- **파일 탐색기 패널 (Ctrl+T)** — TUI에서 워크스페이스 트리를 모달로 열람. ↑↓·jk 이동, Enter로 디렉터 펼치기·파일 미리보기(40줄), ←로 접기, Esc로 닫기. 숨김·빌드 산출물(target, node_modules 등)은 자동 제외. 열려 있는 동안 대화 영역이 좌우 분할됩니다.
- **LSP 고도화** — `lsp_hover`(심볼 타입·문서)·`lsp_references`(참조 위치) 도구 추가. **편집 후 자동 진단**: edit_file·multi_edit·write_file 성공 시 언어 서버 진단을 5초 예산으로 수집해 결과에 `[lsp]` 로 덧붙입니다 — 에이전트가 오류를 스스로 보고·수정하는 자가 검증 루프. 서버가 없거나 늦으면 조용히 건너뛰어 편집을 막지 않습니다 (rust-analyzer 콜드 스타트 등).
- **Diff 승인 뷰** — 편집 승인 프리뷰의 unified diff를 줄 단위 색상 렌더링 (`+` 녹색, `-` 빨강, `@@` 보조색). 변경 전후가 승인 전에 한눈에 보입니다.

## 시작 화면 리디자인

- **공식 로고 배너** — `{ / }` 마크 + RIX 워드마크 락업을 7행 블록 아트로 렌더링. 부팅 시 아래에서부터 한 행씩 점등되고, 글자 위를 반짝임이 스쳐 지나갑니다. 마크 전체에 테마별 accent→code 그라디언트가 적용되고 사선은 항상 선명합니다.
- **RAFIKX HARNESS 마키** — 로고 아래에서 "RAFIKX HARNESS · THE TERMINAL IS NOW A RUNTIME" 텍스트가 흘러갑니다.
- **세션 다이제스트 패널** — 오른쪽에 최근 세션 목록. **↑↓로 선택하고 Enter로 바로 이어하기** (`/sessions` 전체 목록은 별도).
- **하단 정보 유지** — RUNTIME 상태·태그라인·MODEL·WORKSPACE·TIP·키 안내는 하단에 그대로. 라이프사이클 스테이지 리본(◇ CONTEXT ─ PLAN ─ EXECUTE ─ VERIFY)은 헤더에서 배너로 이동해 시작 화면 헤더가 깔끔해졌습니다.
- **배려 장치** — 84×26 미만 터미널은 기존 중앙 배치로 폴백, `reduced_motion` 설정 시 애니메이션 없이 정적 렌더링.

## 수정

- 파일 탐색기를 Esc·q·Ctrl+T로 닫아도 닫히지 않던 결함 수정 (키 처리 후 무조건 상태 복원하던 구조).
- 시작 화면 배너 글리프의 내부 공백이 유실돼 글자가 뭉개져 보이던 렌더링 버그 수정.

## 검증

- 테스트 **352개 전부 통과** (v1.1.3 대비 +1) — LSP 실서버 통합 테스트(rust-analyzer hover·references, tsserver 진단 예산 실측 2.1초) 포함
- tmux 실측: 파일 탐색기 열기/닫기·미리보기, diff 색상(ANSI RGB 확인), 세션 이어하기, 로고 락업·마키·반짝임 동작 확인
- CLI·데스크탑 컴파일 성공

## 업그레이드

기존 설치는 `rafikx update` 로 최신 태그를 따라갑니다.
