# RafikX v1.1.5 — 패치: 시작 화면 로고를 원본 구성으로

시작 화면 배너를 첨부한 공식 로고 이미지와 동일한 구성으로 다듬은 패치 릴리스입니다.

## 변경 내용

- **세로 로고 락업** — 원본 로고와 동일하게 위에 `{ / }` 마크, 바로 아래 `RAFIKX` 워드마크를 배치했습니다. 이전의 가로 락업(마크 옆 RIX 글자)을 대체합니다.
- **작고 묵직하게** — 마크를 콤팩트하게(중괄호-사선 간격 2칸) 묶어 사선이 로고처럼 중괄호에 바짝 붙고, 전체를 좌측 패널 중앙에 모아 렌더링합니다.
- **워드마크 타이포** — `RAFIKX`를 블록 글리프 대신 볼드 + 글자 간격 텍스트로 렌더해 원본 로고의 굵은 산세리프 워드마크와 같은 위계를 만들었습니다. R·I·X 블록 글리프는 제거되어 글리프는 마크 3요소만 남습니다.
- 테마 그라디언트·반짝임·사선 선명(BOLD)·부팅 점출 연출·리본·마키·좌우 구분선·세션 선택은 그대로 유지됩니다.

## 검증

- 테스트 **352개 전부 통과** — 락업 구성(MARK/WORDMARK/폭) 검증으로 갱신
- tmux 실측: 마크 7행 전체 렌더·워드마크 중앙 정렬 확인
- CLI·데스크탑 컴파일 성공

## 업그레이드

기존 설치는 `rafikx update` 로 최신 태그를 따라갑니다.
