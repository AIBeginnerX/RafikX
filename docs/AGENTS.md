# AGENTS.md — docs

- 스택: Markdown
- 역할: 설계·설치·배포 문서와 완료 작업 기록(CHANGELOG-작업기록.md) 보관소. 현행 구현의 기준 문서는 HARNESS_V2_DESIGN.md, 사용 문서는 INSTALL.md.
- 관습: 문서는 구현을 따라가게 하고, 구현을 문서에 맞추지 않는다. 사실 관계는 코드에서 확인한 것만 쓴다 — 없는 기능을 지어내지 않는다. 완료된 작업 기록은 tasks/todo.md 가 아니라 CHANGELOG-작업기록.md 로 옮긴다.
- 금지: 구버전 내용을 표시 없이 방치하지 않는다 — 낡은 문서는 상단에 기준 배너를 단다 (SPEC.md 참조).
- 검증: 링크(파일 경로·명령)가 실제로 존재하는지 확인.
