# 02 — 7축 스코어카드 (Phase 2)

> 채점 기준: 0=없음 · 1=개념만 · 2=부분 구현(우회 가능) · 3=구현됨(엣지 취약) · 4=구현+강제+테스트됨
> 모든 점수에 근거(파일:라인)를 붙인다. 기준 커밋: v1.1.5 (fd75c2e)

## 축 1: 목표 명확화 (Intent Elicitation)

| # | 항목 | 점수 | 근거와 판단 |
|---|---|---|---|
| 1-1 | 해석 후보 나열·모호성 감지 단계 | **1** | 시스템 프롬프트 [의도 게이트]가 "모호하면… 남은 분기점만 짧게 물어본다"고 지시(runner.rs:42-45)하나 코드 장치 없음 — 모델 자율에 의존 |
| 1-2 | 우선순위 질문 인터뷰 루프 | **0** | 질문 루프 코드 없음. 승인 채널(approval)은 도구 승인 전용(agent.rs:120-127) |
| 1-3 | acceptance criteria 포함 SPEC 생성 | **1** | ULW의 goal.md에 완료 기준 저장(ulw.rs:136), 계획의 [완료 기준] 섹션(runner.rs:641-642) — 일반 경로엔 스펙 문서 자체가 없고 형식 강제도 약함 |
| 1-4 | SPEC 승인 게이트·동결 | **0** | 승인 후 동결·변경 통제 코드 없음 (grep "동결\|freeze" — 없음) |
| 1-5 | 비기능 요구 명시 확인 | **0** | 해당 장치 없음 |
| 1-6 | SPEC 변경 통제 | **0** | 해당 장치 없음 |

**축 1 총평: 2/24.** 모호성 처리가 전적으로 프롬프트 문구에 의존한다. ULW의 goal.md가 유일한 실체다.

## 축 2: 계획 시스템 (Planning)

| # | 항목 | 점수 | 근거와 판단 |
|---|---|---|---|
| 2-1 | 계층적 분해 | **2** | 계획 텍스트 + todo 도구(tools_more.rs TodoItem) + 그래프 모드 DAG(runner.rs:640). 마일스톤/원자적 스텝 위계는 없음 |
| 2-2 | 태스크 크기 기준 강제 | **1** | 시스템 프롬프트에 "한 태스크 = 하나의 검증 가능한 변경" 계열 지시는 있으나 diff 규모 제한 강제 없음 (constraints.max_diff_lines 개념 부재) |
| 2-3 | 태스크별 실행 가능한 DoD | **2** | 그래프 모드 [완료 기준] 추출(runner.rs:641-642, extract_plan_section) — 텍스트 체크리스트라 실행 가능 명령이 아님 |
| 2-4 | 의존성 명시·순서 결정 | **1** | depends_on 같은 데이터 구조 없음. todo 순서는 모델이 정함 |
| 2-5 | 계획 적대적 비평 패스 | **2** | 계획 프롬프트가 [반박] 섹션 강제(runner.rs:241)하고 검증자가 이를 최대 위험으로 소비(runner.rs:1673-1685). 단, 같은 컨텍스트의 자기비평이며 별도 패스의 공격은 아님. critic 노드는 그래프 모드 한정(runner.rs:1802,1849,1885) |
| 2-6 | 재계획 트리거 | **1** | 검증 실패 2회 시 verify_fail로 종료(runner.rs:1492-1498) — "계획으로 돌아가기" 단계 없음. ULW 재촉(ulw.rs:97)만 존재 |
| 2-7 | (지시서 미정의 항목 — 공란 유지) | — | 원문 그대로 보존 |

**축 2 총평: 9/24.** 계획이 "텍스트 예술"이다. 데이터(태스크 스키마)가 아니라서 검증·순서·크기 강제가 모두 불가능하다.

## 축 3: 거짓 완료 차단 (최우선)

| # | 항목 | 점수 | 근거와 판단 |
|---|---|---|---|
| 3-1 | 완료 판정 주체가 시스템 | **2** | 1차 신호는 모델 종료(agent.rs:444-448 status="ok"). 시스템 검증(runner.rs:1255 run_verify)이 뒤따르지만 binding.verify 꺼진 프로파일+기본 policy(Inherit, engine.rs:176)에선 **스킵된다**. ULW는 강함(ulw.rs:260-335) |
| 3-2 | 증거 원장 (명령·exit code·로그·diff 해시) | **1** | JSONL 로그(applog)와 runs 요약(db.rs:1290-1308: iterations·tokens·status)은 있으나 검증 명령별 exit code·diff 해시 원장 없음. bash 출력은 성공 시 요약만(runner.rs:1480-1483) |
| 3-3 | 컨텍스트 격리된 독립 검증자 | **3** | Strict 게이트 리뷰어가 fresh-context(diff 미첨부, 직접 읽게 함 — runner.rs:1702 주석+review_prompt:1702-1726). 단, Inherit 기본에선 미가동이고 판정불능 시 AcceptUnknown 통과(gate_action)라 엣지 취약 |
| 3-4 | 테스트 무결성 가드 | **0** | `#[ignore]` 추가·테스트 삭제·어서션 약화 감지 코드 없음(01_AUDIT Q3) |
| 3-5 | acceptance test 읽기 전용 잠금 | **0** | tests/acceptance/ 개념 자체가 없음. 경로 차단은 Obsidian Vault뿐(tools.rs:reject_vault) |
| 3-6 | 태스크 완료 시 전체 회귀 | **1** | 자동 검증이 `cargo check`뿐(runner.rs:2484) — 테스트 회귀는 미실행이 기본. binding.verify_command 수동 설정 시에만 가능 |
| 3-7 | AC↔산출물 역추적 | **1** | 그래프 모드 dod_checklist 대조는 존재(runner.rs:642), AC 매핑 매트릭스·자동 검사 없음 |
| 3-8 | 하드코딩·스텁 탐지 | **1** | 시스템 프롬프트가 "스텁·mock·'일단 MVP'는 미완"(runner.rs:87)이라고 말할 뿐 검증 장치 없음. 리뷰어 프롬프트에도 변형 입력 지시 없음(review_prompt:1702-1726에 없음) |

**축 3 총평: 11/32 — 축 3에서 3점 미만 항목 6개(3-2,3-4,3-5,3-6,3-7,3-8) → 전부 자동 Critical.** 시스템의 뼈대(격리 검증자, ULW 게이트)는 좋으나 기본 경로(Inherit)에서는 검증조차 스킵되고, 테스트 무결성·증거 원장이 없어 거짓 완료의 대부분 시나리오가 뚫린다.

## 축 4: 모델 독립적 품질

| # | 항목 | 점수 | 근거와 판단 |
|---|---|---|---|
| 4-1 | provider 어댑터 | **4** | provider/ 모듈(anthropic·openai_compat·gemini 네이티브 폴백 등), config.toml 교체, 폴백 체인(fallback.rs), 19개 provider 실연(auth.rs preferred_models). 코드 수정 없이 설정만으로 교체됨을 v1.1.2 실측(codex 400 수정)으로 확인 |
| 4-2 | 구조화 출력 강제·파싱 재시도 | **1** | lessons.rs:192 JSON 파싱 실패 시 스킵, 검증자 [판정] 파싱(runner.rs:1594)은 구조 미준수 시 본문 fallback. 재시도 루프 없음 |
| 4-3 | 태스크 명세 자기완결성 | **1** | task 위임 시 하위 에이전트가 받는 것은 프롬프트 문자열(tools/task.rs) — SPEC/태스크 문서 개념 없음 |
| 4-4 | 생성→비평→수정 루프·종료 조건 | **2** | 검증 실패 되먹임 재개(runner.rs:1509-1534)와 Strict 재개 1회(gate_action)가 존재. 자기 비평 단계는 프롬프트 수준, 반복 상한은 검증 2회뿐 |
| 4-5 | 품질 게이트 래칫 | **0** | 통과 테스트 수·커버리지 하한 추적 없음 — 후퇴 감지 불가 |
| 4-6 | 모델 보정 스위트 | **0** | 신규 모델 연결 시 능력 프로브 없음 (model_wizard는 ping만 — model_wizard.rs:110-120) |
| 4-7 | 난이도 하네스의 실질 기여 | **3** | 분류→프로파일→tools/max_iterations/verify 차등이 실제 바인딩에 적용(binding.rs, engine.rs:194 Strict 기본). 다만 반복 횟수·검증 강도 조정이 난이도 1차원이고, verify 미설정 프로파일은 스킵 경로 존재 |

**축 4 총평: 12/28.** 어댑터와 난이도 하네스는 자산. 부족한 것은 "품질을 데이터로 강제"하는 층(구조화 출력 재시도, 래칫, 보정)이다.

## 축 5: 오류 최소화와 안전

| # | 항목 | 점수 | 근거와 판단 |
|---|---|---|---|
| 5-1 | 파괴적 명령 차단 | **3** | bash_blocked(tools.rs:884-899): sudo·rm -rf /·~·mkfs·dd 차단, 승인 게이트(기본 ask). 정규화 우회(따옴표·변수 치환) 엣지는 수동 목록이라 취약 |
| 5-2 | git 체크포인트·롤백 | **1** | mutation 롤백(tools/mutation/commit.rs:142-165)은 적용 실패 시 원복만. 태스크 단위 git 커밋·스냅샷 없음 |
| 5-3 | 변경 크기 제한 | **0** | diff 규모 제한·분해 강제 없음 |
| 5-4 | 실행 격리 | **2** | 경로 감옥(tools.rs:resolve_in_workspace)+승인+해시 앵커로 오염 가능성을 줄이나, 테스트 실행이 작업 트리를 오염시키지 않는 샌드박스 구조는 아님 |
| 5-5 | 실패 처리 사다리 | **2** | 검증 재시도 2회→verify_fail(runner.rs:1492), 동일호출 3회 차단(agent.rs:40-48), ULW 재촉·포기(ulw.rs:97). "접근 변경→재분해→에스컬레이션"의 명시적 사다리는 없음 |
| 5-6 | 예산·강제 에스컬레이션 | **3** | AGENT_MAX_ITER 25·HARD_CAP 50(agent.rs:20-21), 검증 3라운드 상한, 토큰 상한 처리(agent.rs:431-434). 비용 예산(금액) 기준은 없음 |

**축 5 총평: 11/24.** 도구 계층 안전은 준수. 부족한 것은 git 체크포인트와 "재시도→접근 변경→재분해→에스컬레이션"의 데이터화된 사다리다.

## 축 6: 상태 관리와 재개

| # | 항목 | 점수 | 근거와 판단 |
|---|---|---|---|
| 6-1 | 상태 전부 파일 영속화 | **2** | 세션 메시지(db), ULW goal.md/state.json(ulw.rs:136), usage.json, 로그. 일반 경로의 계획·증거는 미영속(01_AUDIT Q5) |
| 6-2 | 마지막 검증 지점에서 재개 | **2** | 세션 재개(chat --resume → load_session, tui.rs resume_session_by_id), /ulw-resume(ulw). "검증된 체크포인트" 개념은 없음 — 재개가 곧 마지막 대화 지점 |
| 6-3 | 컨텍스트 압축이 검증 사실만 담음 | **2** | 자동 압축(packer.rs AUTO_COMPACT_PERCENT=80), 요약 프롬프트(chat.rs:1567 "결정·사실·남은 할 일") — 요약이 검증 여부를 구분하지는 않음 |
| 6-4 | 검증된 교훈만 메모리 저장 | **2** | lessons는 실패 트리거에서만 생성(lessons.rs:143)하지만 내용은 모델 반성 텍스트라 "검증"된 것이 아님. 중복 방지·가중치(db FTS)는 우수 |

**축 6 총평: 8/16.** 세션·ULW 재개는 있으나 "검증된 지점"이 상태의 단위가 아니다.

## 축 7: 관측성

| # | 항목 | 점수 | 근거와 판단 |
|---|---|---|---|
| 7-1 | 구조화 로그(JSONL) | **3** | logs/{debug,ops}.log JSONL(applog), graph 노드 이벤트, lifecycle 이벤트(run/events.rs). 모델 호출 원문·도구 입력 전체는 요약 수준 |
| 7-2 | 태스크별 비용·시간·재시도 집계 | **3** | runs 테이블(db.rs:1290-1308: class/provider/iterations/tokens/status), usage.json. 태스크(=세션 실행) 단위라 세부 단계 집계는 아님 |
| 7-3 | 로그만으로 사후 분석 | **2** | 실패 시 verify_fail 사유·graph 노드로 흐름 추적 가능. 검증 명령 출력 원문·exit code 가 남지 않아 재현성은 떨어짐(runner.rs:1480 성공 시 요약만 저장) |

**축 7 총평: 8/12.** 뼈대 훌륭. 증거 원장으로 승격하면 4점 행렬이 된다.

## 종합

| 축 | 점수 | 만점 | 비율 |
|---|---|---|---|
| 1 목표 명확화 | 2 | 24 | 8% |
| 2 계획 | 9 | 24 | 38% |
| **3 거짓 완료 차단** | **11** | **32** | **34%** |
| 4 모델 독립 품질 | 12 | 28 | 43% |
| 5 안전 | 11 | 24 | 46% |
| 6 상태/재개 | 8 | 16 | 50% |
| 7 관측성 | 8 | 12 | 67% |

**핵심 결론**: 관측성(67%)·안전(46%)은 이미 상위 수준이고, 목표 명확화(8%)와 거짓 완료 차단(34%)이 바닥이다. 특히 축 3의 부재는 "나머지 기능의 신뢰성"을 무효화한다 — 지시서의 우선순위 원칙(검증 먼저)이 코드 감사 결과와 정확히 일치한다.
