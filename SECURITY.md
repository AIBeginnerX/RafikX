# 보안

RafikX는 API 키와 봇 토큰을 **설정 폴더의 비밀 파일**에만 둡니다. `config.toml`·채팅·이슈·로그에 원문을 붙이지 마세요.

## 비밀이 새었을 때

1. 해당 프로바이더 콘솔에서 키를 폐기하고 새로 발급합니다.
2. 텔레그램 봇이면 BotFather에서 토큰을 재발급합니다.
3. `~/.rafikx/secrets.toml` 과 `~/.rafikx/auth.json` 을 확인합니다.

## 취약점 신고

공개 이슈에 공격 PoC나 탈취된 키를 올리지 마세요. 저장소 관리자에게 비공개로 알려 주세요.

## 프로그램이 지키는 것

- 파일/명령은 설정한 워크스페이스 밖을 나가지 못합니다.
- 텔레그램 허용 목록 밖 사용자는 응답하지 않습니다.
- 원격(텔레그램)에서 `--yes` 무조건 승인은 코드가 막습니다.
- Inspector는 코드와 설정을 자동으로 고치지 않습니다.
