# RafikX (crate)

이 폴더는 Rust 크레이트입니다. **사용자 안내·설치는 저장소 루트 [README.md](../README.md) 와 [docs/INSTALL.md](../docs/INSTALL.md)** 를 보세요.

```bash
# 저장소 루트에서
cargo install --path agent-harness --force
rafikx
```

패키지 이름 `rafikx`, 명령 `rafikx`. Windows · macOS · Linux.
