# RafikX v1.1.3 — 패치: 모델 작업 표시 개선

모델이 작업하는 동안 보이는 출력의 언어와 스타일을 다듬은 패치 릴리스입니다.

## 변경 내용

- **작업 출력 언어 분리** — 작업 중 진행 설명(생각·중간 점검·단계 서술·도구 호출 앞뒤 안내)은 영문으로 출력하고, 핵심 내용·진행 상황의 제목(헤더)만 한국어로 씁니다. 최종 답변(작업 결과 브리핑)은 기존처럼 한국어로 종료됩니다.
- **모델 작업 시각 구분** — TUI에서 턴 진행 중의 모델 출력(스트리밍 텍스트·작업 중 엔트리)을 흐린 회색+기울임으로 렌더링합니다. 작업 중 제목(헤더)은 굵게+기울임으로 남겨 한국어 제목이 눈에 걸립니다.
- **최종 답변은 보통 스타일 유지** — 턴이 끝나면 작업 엔트리가 정리되고 남은 브리핑은 기존과 같은 선명한 스타일로 렌더링됩니다.
- 렌더 캐시 키에 작업 여부 플래그를 반영해 진행 중/완료 렌더가 섞이지 않게 했습니다.

## 검증

- 테스트 **355개 전부 통과** (v1.1.2 대비 +4: 언어 정책 프롬프트·작업 기울임·제목 굵게 유지·스트리밍 테일)
- CLI·데스크탑 컴파일 성공

## 업그레이드

기존 설치는 `rafikx update` 로 최신 태그를 따라갑니다.
