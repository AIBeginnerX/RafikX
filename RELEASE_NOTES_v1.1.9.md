# RafikX CLI v1.1.9 — 코딩 에이전트 복구

영문·한글 개발 요청과 재시도를 도구 가능한 개발 레인에 유지하고, 도구·검증·종료 상태를 실제 실행 증거로 판정하도록 하네스를 복구한 패치 릴리스입니다.

## 사건과 원인

- 영문 산출물 요청이 `Simple`로 분류되어 파일 도구 없이 종료됐고, `다시 해줘` 같은 후속 요청도 이전 개발 맥락을 잃었습니다.
- Bash의 비정상 종료가 성공 도구 결과로 전달되고, Bash가 만든 파일은 변경 증거에 포함되지 않았습니다.
- 목표 연속 실행과 검증 복구가 반복 예산을 각각 새로 받아 한 요청이 87~149회까지 늘어났습니다.
- 병렬 위임의 하위 에이전트가 각자 반복 예산을 새로 받았고, 실패·제한·취소된 하위 작업도 성공 도구 결과처럼 부모에게 전달되어 부모가 정상 완료로 끝날 수 있었습니다.
- 정상 게임도 짧은 중복 휴리스틱에 막혔으며, 미완료 단발 실행은 종료 코드 0을 반환했습니다.

## 수정

- **개발 요청 라우팅** — `create`, `write`, `fix`, `edit`, `add`, `remove`, `delete`, `rename`, `replace`, `upgrade` 등 영문 개발 동사와 앱·버튼·함수·모듈·페이지·테스트 같은 산출물 조합, HTML/CSS 확장자를 `Dev`로 분류합니다. 바로 앞 개발 턴의 재시도만 개발 레인을 상속하고, 사용자가 강제한 `simple` 분류는 자동 승격하지 않습니다.
- **도구 실행 계약** — Bash 비정상 종료를 오류로 전달하고, 명령 전후 워크스페이스의 결정적 전체 내용 해시 차이로 생성·수정·삭제·동일 크기 재작성·ignore 파일을 변경 증거에 기록합니다. 파일·항목·단일 파일·전체 해시 양·시간 상한을 두며, 추적할 수 없는 작업은 실행 전에 닫힌 실패로 중단합니다. 내용 해시에서 제외한 `.cache`, `.git`, `node_modules`, `target` 등의 내부도 경로 메타데이터는 순회해 워크스페이스 밖 링크를 Bash 실행 전에 차단합니다. stdout·stderr는 캡처 상한 뒤에도 EOF까지 동시에 비워 유한한 대량 출력이 파이프에서 막히지 않으며, 보관분만 20KB로 자릅니다. 모든 도구의 성공 출력·오류·승인 미리보기·검증 명령과 최종 검증 이력은 실시간 UI, 실행 로그, 콜백, 상위 모델의 도구 결과와 목표 저장 상태에 들어가기 전에 ANSI·OSC·자격 증명·로컬 경로를 정제하고 길이를 제한합니다.
- **최종 변경 증거** — 처음 변경하기 전의 내용과 종료 시점의 내용을 비교합니다. 수정 후 원복은 변경으로 가장하지 않고, `touch`만 한 파일도 변경으로 오인하지 않으며, 성공·실패와 무관하게 하위 작업의 기준선을 부모 실행에 전파합니다. macOS의 `/tmp`와 `/private/tmp`처럼 심볼릭 링크로 표현이 달라지는 워크스페이스도 같은 물리 경로로 정규화하되, 최종 심볼릭 링크 자체는 대상 파일로 치환하지 않고 변경 증거의 경로 정체성으로 보존합니다. 내부 심볼릭 링크의 생성·삭제·대상 변경도 별도 증거로 기록하고, 워크스페이스 밖을 가리키는 링크는 Bash 실행 전에 차단합니다.
- **턴 단위 종료 예산과 순서** — 계획·일반 연속 실행·병렬 하위 에이전트·3~7개 그래프 노드와 재시도·품질 복구·독립 검증자가 원자적으로 청구하는 최대 50회의 단일 예산을 실행 트리 전체에서 공유합니다. 병렬 위임도 이 상한을 넘을 수 없고 최종 반복 수는 루트와 모든 하위 실행의 실제 합계입니다. 그래프가 존재하지 않는 선행 노드를 참조하면 간선을 조용히 버리지 않고 계획 전체를 기본 파이프라인으로 폴백합니다.
- **위임 실패 정직성** — 하위 작업은 `ok`일 때만 성공 도구 결과가 됩니다. 실패·미완료·거부·취소·제한과 알 수 없는 상태는 정제·상한 처리된 오류로 부모에게 전달하고, 부모가 같은 입력의 위임을 실제 성공으로 재시도하기 전까지 미해결 상태를 유지하므로 텍스트만으로 정상 완료를 선언할 수 없습니다. 직접 provider 오류도 lifecycle 저장·실시간 발행·부모 도구 결과보다 먼저 비밀값을 제거하고 길이를 제한합니다. OpenAI 호환·Anthropic HTTP 오류는 상태와 `Retry-After`만 보존하고 신뢰할 수 없는 응답 본문을 읽거나 오류 문자열에 넣지 않으므로, 서버가 요청 자격 증명을 무표식으로 되돌려도 메모리와 영구 디버그 로그에 남지 않습니다. 실패한 하위 작업이 만든 파일 변경 증거는 오류와 별개로 계속 부모에게 전파됩니다.
- **검증 복구** — 명시적 검증 명령이 성공해도 변경 파일 전체의 품질 게이트를 반드시 이어서 실행하며, Rust와 JavaScript가 함께 바뀌면 Rust가 주 언어여도 각 JavaScript 파일을 Node로 검사합니다. 혼합 Rust·Python·Go·TypeScript·TSX·JSX 변경도 각 비주력 언어의 구문 검사를 생략하지 않고, 필요한 검사기가 없으면 닫힌 실패로 처리합니다. HTML/JS 품질 실패를 같은 턴에서 수정·재검증합니다. 기본 rafikx 엔진은 개발 작업에서 신선한 읽기 전용 검증자를 거치며, 검증자 통과 뒤에는 자동 수정을 허용하지 않는 최종 기계 검증을 실행해 리뷰 이후 파일이 바뀐 채 성공하는 순서 역전을 막습니다. 변경된 모든 HTML과 변경 웹 소스의 상위 엔트리, 비정형 이름의 HTML, 실제 파일 참조, 로컬 모듈의 전이 import, 같은 패키지의 대표 엔트리를 제한된 탐색으로 찾아 각각 실제 브라우저에서 엽니다. 브라우저 게임 작업은 같은 공유 스크립트를 참조하는 모든 HTML 엔트리를 끝까지 탐색하므로 먼저 찾은 `index.html`이 다른 게임 페이지를 가리지 못합니다. 공유 CSS만 참조하는 일반 페이지는 일반 브라우저 스모크만 받고, 게임 표식·표면·단일 게임 엔트리로 확인된 페이지만 게임 상태 계약을 받습니다. 상위 디렉터리의 무관한 `index.html`은 변경 소스를 실제 참조할 때만 진입점으로 인정하므로 중첩 앱의 런타임 오류를 가리지 못합니다. 변경된 HTML/CSS를 실행할 엔트리가 없거나 참조 그래프를 제한 안에서 증명할 수 없으면 닫힌 실패로 처리합니다. `public/index.html`과 `src/app.js`처럼 엔트리 바깥의 프로젝트 자산도 상대 경로를 보존해 스테이징합니다. HTTP로 실제 모듈을 로드하고 `console.error`, 예외, Promise 거부, 리소스 404를 수집하며 브라우저 실패·미설치를 통과로 숨기지 않습니다. Snake·Tetris·Pong·Breakout·Flappy Bird·Super Mario·일반 HTML 게임과 playable·interactive mode 요청을 포함한 브라우저 게임 의도는 작업 문장에서 공통 분류해 검증 계약을 강제하며, 유명 브라우저 게임 요청은 HTML을 명시하지 않아도 인식하고 Rust·Unity·Pygame 같은 명시적 네이티브 대상을 침범하지 않습니다. 게임과 차트를 함께 요청해도 게임 계약을 유지합니다. Canvas뿐 아니라 SVG·DOM 게임도 대상이며, 단일·다중 엔트리의 실제 게임 페이지만 상태 전이 계약으로 검사합니다. 실제 키 입력으로 `ready → playing → paused → playing → lost → ready`와 재시작 횟수를 검사합니다. 상태 전이는 보이는 텍스트·스타일·레이아웃·Canvas 픽셀·SVG 도형으로 확인하되, `playing` 진행 판정에서는 DOM 텍스트와 Canvas 2D의 `fillText`·`strokeText`를 제외하고 프레임별 도형·이미지 그리기, SVG 도형, 텍스트 없는 실제 도형 요소의 변화만 인정합니다. 따라서 `data-*` 속성·숨은 노드·DOM 또는 Canvas의 상태·점수·카운터 문구만 바꾼 가짜 API는 통과하지 못하며, 게임 루프 또는 ArrowRight/KeyD 조작으로 주 게임 표면 자체가 실제 진행되어야 합니다. `lost`가 아닌 상태의 `KeyR`이 상태나 횟수를 바꿔도 실패합니다.
- **검증 보안** — `--yes`도 Bash 안전 차단을 우회하지 못하며, 분리·결합 재귀 플래그, `--`, 따옴표, `sh -lc`·`.exe` 셸·PowerShell Command 중첩, `eval`, 삭제 명령 변수 할당과 Unix 홈·루트 및 Windows `%USERPROFILE%`·드라이브 루트 삭제를 정규화해 차단합니다. 브라우저에는 선택된 HTML에서 실제로 도달 가능한 `src`·`href`·CSS `url()`·모듈 import·`fetch()`·스프라이트 `.src` 자산만 전이 그래프로 임시 스테이징하고, dotfile·자격 증명 파일과 워크스페이스 밖 링크를 제외합니다. 참조 symlink는 별칭뿐 아니라 canonical 대상의 숨김·민감명·제외 경로·파일 형식도 다시 검사하며, 민감한 자산을 명시적 이름이나 평범한 별칭으로 참조해도 닫힌 실패로 중단합니다. 자산 수·파일·합계·시간과 브라우저 stderr·오류 수에 상한을 둡니다. stderr 상한 초과와 리더 종료 지연은 실패로 처리하고, 샌드박스 CSP와 현재 임시 서버 포트만 허용하는 오프라인 프록시로 외부 및 다른 로컬 서비스 통신을 차단하며 모든 종료 경로에서 서버·프로필을 정리합니다. 자동 품질 명령과 의존성 감사는 셸 문자열 대신 프로그램·인자 배열로 실행하고, stdout·stderr를 고정 상한으로 수집합니다. Windows는 명령을 정지 상태로 시작해 kill-on-close Job Object에 넣은 다음 재개하므로 breakaway를 허용하지 않은 전체 자손을 커널 경계로 정리합니다. Unix는 프로세스 그룹·부모관계·실행별 환경 표식·비공개 상속 FD를 교차 확인하고, 조회가 실패해도 그룹·루트·이미 찾은 PID의 종료와 비동기 회수 확인을 끝까지 시도한 뒤 오류를 반환합니다. SIGKILL 전달 성공은 커널 전달 영수증으로 판정하고, 최종 조회에서 뒤늦게 발견된 PID에도 신호를 전달한 뒤 다시 확인해 종료 중인 단기 프로세스를 실패로 오인하지 않되 현재 PID에 대한 실제 전달 실패는 오류로 남깁니다. 이 Unix 경로는 일반 코딩 명령의 누수를 줄이는 운영 안전장치이며, 환경과 모든 상속 FD를 의도적으로 지운 뒤 daemonize하는 적대 프로세스를 가두는 보안 샌드박스는 아닙니다. Rust 포맷에는 Rust 변경 파일만 전달하고 ANSI 색상을 제거한 Clippy 결과를 변경 파일에 귀속해 기존 저장소 부채와 새 회귀를 구분합니다. `cfg(test)` 모듈과 의미상 테스트 디렉터리·파일만 제외하며 `contest.rs`, `latest.rs`, `example_client.rs` 같은 운영 파일은 계속 스캔합니다. 주석 안의 가짜 `#[cfg(test)] mod`는 운영 코드 마스크를 바꾸지 못하고, `r`·`br`·`cr` raw string 뒤의 운영 코드도 계속 검사하며, 정확한 `// SAFETY:` 접두사 뒤에 비어 있지 않은 근거가 없는 `unsafe` 토큰은 실패시킵니다. 필수 도구가 없으면 닫힌 실패로 처리합니다. 패키지 없는 순수 JavaScript도 Node 구문 검사와 실제 브라우저 게이트가 모두 있어야 통과하며, Node가 없거나 변경된 HTML/CSS의 엔트리가 없으면 실패합니다. 실패 증거와 실시간 출력의 명령·메모·파일 경로를 필드별·전체 길이 상한으로 제한하고, 로컬 경로·공백·줄바꿈 포함 대입형 자격 증명·ANSI로 분할된 민감 키·장식형 인증 헤더·URL 쿼리·URL 사용자 정보·URL 경로의 Telegram·AWS·OpenAI·GitHub·Slack·고엔트로피 토큰을 정제하며, 비정상 UTF-8 Bash 출력도 문자 경계에서 안전하게 자릅니다.
- **상태 정직성** — 도구 호출 문법 누출의 개발 승격이 실패하거나 개발 파일 변경이 없으면 완료로 보고하지 않습니다. 차단되거나 거부된 검증은 성공으로 사라지지 않고, 그래프 노드의 재시도 결과도 다음 노드로 넘기기 전에 다시 검증합니다. Cargo·Python 자동 명령이 없는 HTML/JavaScript 노드도 구문·브라우저·보안 경계를 통과해야 하며, 경계 검증이 두 번 실패한 노드는 에이전트 본문 상태가 `ok`여도 그래프 전체를 `fail`로 끝냅니다. 검증자 수정 뒤에는 기계 검증을 다시 실행합니다. 빌드·테스트·품질 게이트는 상위 하네스가 실행하고, 읽기 전용 검증자는 `read_file`, `list_dir`, `grep`, `glob` 네 도구를 누락·중복 없이 정확히 받아 그 시스템 상태와 직접 읽은 코드·계약을 대조합니다. 이름 확장으로 `todo_write` 같은 변경 도구가 섞일 수 없고, 실행을 요구하면서 Bash를 주지 않던 모순도 없습니다. 검증자는 매 시도마다 고유한 자식 실행 문맥을 받고 해당 시도의 성공한 `read_file`·`grep` 증거로 파일을 직접 읽어야 하며, 시스템 지시를 되묻는 답이나 이전 시도의 열람 흔적만으로 `PASS`할 수 없습니다. 검증자가 `PASS` 도중 파일을 바꾸거나 검증자 바인드·전송·재개·변경 추적이 실패해도 활성 목표를 닫힌 실패로 종결합니다. 단일 검증자의 5개 그룹 판정은 정확성·보안·성능·가독성·API 설계 중 하나라도 실패하거나 누락되면 실제 실패로 집계합니다. `rafikx ask`와 `rafikx agent`의 미완료·실패·거부·취소는 비정상 종료 코드로 반환합니다.
- **Codex 백엔드 호환성과 비용 가드** — ChatGPT Codex 응답 백엔드가 거부하는 `max_output_tokens` 필드는 전송하지 않되, 스트림·비스트림 응답 모두에서 서버가 보고한 실제 출력 토큰을 설정된 `max_tokens`와 대조합니다. 사용량 보고가 없거나 상한을 넘긴 응답의 도구 호출은 실행하지 않고 미완료로 되먹이며, 텍스트 바이트 수를 토큰으로 오판하지 않습니다. 패커와 실제 요청은 시스템 프롬프트·도구 스키마의 고정 입력 비용을 먼저 차감하고 모델 컨텍스트 창 안에서 실제 `max_tokens`와 메시지 예산을 같은 값으로 계산합니다. 고정 입력만으로 출력 여유가 사라지거나 메시지 예산이 0이면 현재 사용자 작업을 버린 빈 요청을 보내지 않고 provider 호출 전에 닫힌 실패로 중단합니다. 한 메시지에 여러 도구 결과가 있어도 전체 결과 예산을 형제 블록에 결정적으로 나누고, 가능한 결과에는 앞·생략 표식·뒤를 모두 남깁니다. 도구 결과 본문을 빈 내용 고정점까지 줄여도 완전한 도구 호출·결과 쌍과 생략 안내가 함께 예산에 들어가지 않으면 그 쌍 전체를 원자적으로 제외하고 생략 안내 하나를 남깁니다. 도구 호출·결과의 비어 있지 않은 ID, 개수, 중복 여부와 정확한 집합을 검증해 불일치·부분 쌍은 제거하되 함께 있던 일반 텍스트는 보존합니다. 살아남은 쌍의 순서·메타데이터·오류 상태를 보존하며 반복 압축 뒤에도 요청 전체가 상한 안에 들어갑니다. 하네스 내부의 50회 턴 예산과 종료 조건도 그대로 유지됩니다.
- **공급자 스트림과 최종 답변 경계** — Anthropic SSE의 block index별 `text`·`tool_use`, 분할된 `input_json_delta.partial_json`과 도구 입력 진행률을 끝까지 모으고 block 종료 시에만 완전한 JSON을 도구 호출로 확정합니다. 잘리거나 잘못된 입력과 출력 상한에서 미완성인 도구 호출은 실행하지 않습니다. 공개 `StreamEvent`와 기존 fallback API는 유지하면서 내부 의미 스트림이 추론, 아직 확정되지 않은 본문 후보, 도구 인자 진행을 구분합니다. 작업 중 후보 문장은 화면·세션·`Answering` 상태에 노출하지 않고, 도구 없는 턴의 도구 문법 누출과 구조화 `ToolUse`도 거부합니다. 최종 답변이 확정되면 본문보다 먼저 `Answering` lifecycle을 발행하고, Codex의 `[DONE]`은 열린 추론 구간을 닫습니다.
- **동시 첫 실행 설정** — 여러 터미널 프로세스가 같은 빈 데이터 폴더에서 동시에 시작해도 완성된 기본 config를 같은 폴더의 임시 파일에 기록한 뒤, 기존 파일을 교체할 수 없는 원자적 hard-link 게시로 최초 파일만 채택합니다. 사용자가 동시에 저장한 config는 보존하고 병렬 테스트도 각각 고유한 설정 폴더를 사용합니다.
- **동시 실행 데이터베이스** — 여러 터미널 세션 또는 병렬 테스트가 같은 SQLite 파일을 동시에 열어도 WAL 전환의 일시 잠금에 안전하게 양보하고, 마이그레이션 전체를 즉시 쓰기 트랜잭션 하나로 직렬화해 중복 버전 삽입과 부분 적용을 막습니다.
- **크로스플랫폼 업데이터** — `sh` 스크립트 의존성을 제거하고 Rust에서 정확한 `refs/tags/vX.Y.Z`만 fetch·detached checkout한 뒤 HEAD와 태그 커밋을 다시 대조합니다. GitHub API fallback의 태그 목록에서도 annotated tag의 peeled `^{}` ref, prerelease, 임의 접두사를 제외하고 설치기가 수용하는 정확한 안정 태그만 고릅니다. 비공개 저장소용 GitHub 자격 증명 옵션은 Git 하위 명령 앞에 배치하고 이 fetch 한 번에만 환경으로 전달하며, checkout·검증·빌드에는 제거합니다. Unix는 검증된 소스에서 바로 설치하고, Windows는 별도 루트에 빌드한 실행 파일을 현재 프로세스 종료 후 PowerShell로 교체·정리합니다.
- **Canvas 게임 증거 강화** — Canvas 진행은 제한된 2D 도형·이미지 연산의 의미 변화와 최대 128×128로 축소한 실제 픽셀 변화가 함께 관측될 때만 인정합니다. 게임 표면 자신뿐 아니라 DOM·Shadow DOM 조상의 `display`·`visibility`·`content-visibility`·`opacity`를 확인해 투명한 컨테이너 아래의 화면은 거부합니다. `fillText`·`strokeText`, clear-only, alpha 0, 투명 색·그라디언트·패턴, 투명 `ImageData`, 불완전 path, 추적되지 않은 WebGL·bitmaprenderer·ImageBitmap은 닫힌 실패로 제외합니다. 반대로 숫자 행렬과 `DOMMatrix2DInit`을 구분하는 `Path2D.addPath`, 실제 2D `OffscreenCanvas`, crop·resize 인자를 포함해 고정한 `createImageBitmap`, `transferToImageBitmap` 도형을 실제 진행으로 인정합니다. 대상뿐 아니라 원본 Canvas에도 크기·총 픽셀 상한을 적용하며 연산 수·문자열·path 명령·샘플을 제한합니다.
- **브라우저 증거 인증과 가림 판정** — 페이지가 출력할 수 있는 콘솔 문자열을 성공 근거로 사용하지 않습니다. 실행마다 난수 capability를 만들고, 작성된 스크립트보다 먼저 주입한 일회성 probe가 캡처한 native `fetch`로 오류·게임 완료·준비 영수증을 로컬 서버에 전송합니다. 잘못된 capability, 순서 위반, 재전송과 probe 재요청은 거부합니다. 준비·게임 완료는 최초 한 번만 상태를 바꾸고, 오류 상세는 300자로 정규화한 뒤 중복을 거부하며 실행당 64개까지만 보관하므로 늦거나 반복·폭주하는 콜백이 판정과 메모리를 덮어쓰지 못합니다. Canvas/SVG/DOM 표면은 viewport 안의 여러 점에서 `elementFromPoint`로 실제 노출을 다시 확인해 완전히 덮인 게임을 실패시키되 일부가 보이는 표면은 허용합니다. Windows는 사용자·시스템 `LOCALAPPDATA`·`PROGRAMFILES` 계열 Chrome/Edge와 PATH를 탐색합니다. Canvas/SVG 차트의 score·controls·pause·level 같은 일반 문구만으로 게임 계약을 강제하지 않습니다.
- **터미널 시작 화면과 실제 PTY QA** — 부팅 중 디지털 로고·단계 신호·마키를 보여주고 정착 뒤에는 연결 상태에 따라 `RUNTIME ONLINE` 또는 `CONNECT A MODEL TO OPEN THE RUNTIME`으로 고정합니다. Ready 뒤에는 부팅 tick과 반짝임을 멈춰 대기 화면이 다시 움직이지 않습니다. Unicode 실제 셀 폭과 이미 그린 헤더 요소의 폭을 함께 계산해 남은 칸만 워크스페이스에 배정하고, 좁은 화면에서는 `…/` 뒤의 한글 마지막 디렉터리와 완결된 `팁 더 보기 · /tips`를 보존합니다. 진행 사고 색은 `#11100e` 배경에서 약 4.69:1인 `#817e77`로 조정해 보조 계층과 WCAG AA 대비를 함께 유지합니다. 하단 상태는 `Booting`, `Ready-configured`, `Ready-unconfigured`, `Queued`, `Planning`, `Running`, `Waiting approval`, `Delegating`, `Answering`, `Cancelling`과 각 종료 결과를 이름으로 직접 표시합니다. lifecycle 구독이 이벤트 지연으로 lagged 되어도 최신 RunContext 상태로 다시 동기화하며, 재사용된 ULW 루트도 실행마다 새 epoch를 받아 이전 턴의 늦은 종료 이벤트가 `/compact`·역할 배정·모델 갱신을 덮지 못합니다. 격리된 임시 홈·한글 워크스페이스의 실제 CLI 바이너리를 xterm.js PTY로 구동해 60·80·100·120열, 연결 없는 reduced-motion, 실행·취소·승인·진짜 성공 요약과 실제 HTTP 500 실패를 포함한 9개 상태를 PNG·텍스트·ANSI·메타데이터로 캡처합니다. 취소 증거는 안정된 `Cancelled` 화면을 남기면서 원시 PTY 스트림에서 순간 `Cancelling` 상태와 취소 요청 문구도 확인합니다. PTY 자식에는 명시적 allowlist 환경만 전달해 호스트의 키·토큰을 상속하지 않습니다. QA 러너는 동적 mock 포트와 실행별 staging 폴더를 사용하고 모든 실패 경로에서 PTY·브라우저·서버·실행별 `RAFIKX_HOME`·임시 config를 정리한 뒤, 전체 성공 때만 고정 증거 파일을 교체합니다.
- **검증 증거와 포매터 정직성** — 태스크 검증 명령·오류·stdout·stderr는 영구 증거와 터미널 요약에 들어가기 전에 자격 증명과 로컬 경로를 제거하고, 정제 후 마지막 20줄·2,000자로 제한해 멀티라인 비밀값이 절단 경계로 새지 않게 합니다. Rework 요약은 `시간 초과`와 `출력 상한 초과`의 첫 원인을 보존하며, 명령 실패가 확정되면 판정을 바꾸지 못하는 Git diff 수집을 건너뜁니다. `gofmt -l/-d`와 `shfmt -d`가 종료 코드 0으로 차이를 stdout에 출력해도 포맷 실패로 판정합니다.
- **업데이트 커밋 고정** — GitHub API가 알려준 태그도 정확한 안정 태그 ref 목록에 존재해야 사용자에게 표시합니다. lightweight·annotated 태그를 불변 40자리 커밋 OID로 해소해 확인 시점부터 설치까지 전달하고, 설치 직전 다시 해소한 OID가 달라졌으면 checkout·빌드 전에 중단합니다. 중첩 태그 접두사, 충돌 ref, peeled-only ref와 이동한 태그는 거부합니다.
- **동시 CLI·프로세스 안정성** — Unix scope marker의 부모 FD를 spawn 직후 닫고 scope spawn 구간을 직렬화해 병렬 명령이 서로의 후손으로 오인되는 창을 제거했습니다. Linux는 pidfd, macOS는 커널 pid-version audit token으로 발견 당시의 정확한 프로세스 세대에만 STOP/KILL을 전달합니다. macOS 신호가 `EPERM`과 대상 종료 사이에서 경합하면 즉시 `unique_id`와 `pidversion`을 다시 읽어 원래 세대가 이미 사라졌음이 증명될 때만 오류를 억제하고, 같은 세대이거나 재조회가 실패하면 원래 오류를 유지합니다. 종료가 시작되면 `Child`와 `ProcessScope` 소유권을 독립 정리 작업으로 넘기므로 호출자 future가 취소돼도 확보한 자식·scope·marker·프로세스 세대 신원이 사라지지 않습니다. 정리 세션 입장 상한과 실제 프로세스 탐색 상한을 분리해 포화된 세션이 스캔 허가를 점유한 채 대기하지 않으며, pre-KILL의 중복 전체 스캔을 제거했습니다. 루트 프로세스 그룹을 먼저 정지한 뒤 환경·marker FD·계보·자손을 찾는 비용 큰 전체 탐색은 완전한 라운드 기준 최대 2개까지만 동시에 실행합니다. 최초 scope 신원 캡처는 탐색 허가를 얻은 뒤 시작해 큐 대기 시간을 8초 정리 구간에 포함하지 않으며, 부모관계와 환경을 지운 자손도 찾을 수 있는 marker-FD 조회를 먼저 실행하고 발견한 각 세대를 즉시 STOP합니다. 그 뒤 신호·재검증·회수 구간만 8초, 시간초과 폴백은 3초로 제한합니다. Linux marker FD의 blocking 조회는 비동기 대기자가 취소되거나 시간 초과돼도 실제 조회가 끝날 때까지 같은 탐색 허가를 보유하므로 실행 중인 전체 탐색 수가 상한을 넘지 않습니다. 환경 표식 탐색은 초기·수렴·최종·폴백 단계에서 계속 실행하므로 부모관계와 scope FD를 모두 지운 뒤 재부모화된 협조적 자손도 놓치지 않습니다. Linux `/proc/*/fd` 조회는 blocking worker와 절대 시간·항목 상한을 사용하고, macOS는 반복 `lsof` 대신 `proc_listpidspath`·`proc_listallpids`와 unique-parent 계보를 사용합니다. Unix 경계는 협조적 코딩 명령의 누수 방지이며 환경과 모든 상속 FD를 함께 지운 적대적 daemon을 가두는 보안 샌드박스는 아닙니다. 실행 ID에는 PID 경계를 넣어 같은 밀리초의 별도 CLI가 Chrome 프로필·임시 경로·DB 식별자를 충돌시키지 않습니다.
- **검증 명령 격리** — 태스크의 임의 검증 명령은 품질 게이트와 같은 제한 출력 수집기와 프로세스 scope에서 실행합니다. stdout·stderr 폭주는 고정 상한 뒤에도 파이프를 비우되 검증 실패로 처리하고, 시간초과·대기 오류·정상 루트 종료 뒤 남은 협조적 분리 자손까지 정리합니다. 검증 명령은 호출자가 선택한 작업 디렉터리에서 실행하며 Windows는 `cmd`, Unix는 `sh`를 사용합니다. 무결성 판정용 Git diff도 같은 시간·출력 상한을 넘으면 닫힌 실패로 중단합니다.
- **릴리스 순서 고정** — 정확한 후보 SHA의 코드·보안·아키텍처·계약·수동 QA 5개 검증을 모두 통과한 뒤에만 push하고, 그 SHA의 Rust 1.96 및 Ubuntu·Windows·macOS CLI CI가 성공한 뒤 같은 SHA에 annotated 태그와 GitHub Release를 게시합니다. 설치된 이전 CLI의 `rafikx update`까지 릴리스 완료 조건에 포함합니다.
- **도구체인 계약** — 기본 Telegram 기능의 잠금 의존성까지 포함한 실제 최소 Rust 버전 1.96을 Cargo manifest에 명시하고, 별도 Ubuntu MSRV CI에서 기본·최소 기능 구성을 모두 같은 버전으로 강제합니다.

## 검증

- 기본 기능 라이브러리 테스트 590개와 `--no-default-features` 라이브러리 테스트 522개가 통과했고, 두 구성의 CLI·lifecycle·mutation·공개 계약·복구 계약·RPC·실행 문맥 통합 테스트도 모두 통과했습니다. 환경 의존 LSP 실동작 테스트 3개와 릴리스용 브라우저 게임 E2E 1개, 합계 4개는 기본 실행에서 ignore 상태이며, 브라우저 게임 E2E는 이번 릴리스 게이트에서 Node와 Chrome을 사용해 1/1이 28.44초에 별도로 통과했습니다. browser 모듈 테스트 50개와 macOS에서 활성화된 process-tree 모듈 테스트 18개, retained-scope 자손 정리와 탐색 허가 포화 회귀, CLI 시간초과·출력 상한·정상 검증 회귀도 통과했습니다. 실제 xterm.js PTY 실행은 9개 화면의 기대 열 수·최대 셀 폭·테두리 정렬을 확인하고 `Booting`, 두 준비 상태, 실행·승인·성공·실패·취소를 증명했으며, 별도 첫 최종 답변 프레임에서 `Answering`을 확인했습니다. 실행 홈·임시 config·staging·promotion lock도 남기지 않았습니다. 릴리스 바이너리는 정확히 `RafikX 1.1.9`를 출력하며 전체 기능 Clippy는 경고와 함께 종료 코드 0으로 통과했습니다. 독립 코드·보안·아키텍처·계약·수동 QA 및 시각·CJK 검토는 구현 검증과 구분해 push 직전 정확한 후보 SHA에서 실행합니다. 회귀 테스트는 깨진 JavaScript·TypeScript·TSX·JSX를 `S3-syntax`에서 거부했고, 실제 CLI 혼합 Rust·TypeScript 게이트도 오류를 차단한 뒤 수정 시 Rust·TypeScript·감사·전체 테스트를 함께 통과했습니다. 이번 릴리스 단계는 CLI만 검증했으며 Tauri 데스크탑은 별도 후속 단계로 보류했습니다.
- 개발 중 후보 `040e2aa`에서는 실제 `rafikx ask`로 패키지 없는 마리오 스타일 브라우저 게임 3개 파일을 생성했습니다. 생성 에이전트의 Chrome CDP 검증에서 좌우 이동·점프·적 밟기·피해·패배·승리와 양쪽 재시작을 통과했고, 별도 상호작용 점검에서도 이동·점프·적 밟기·피해·패배·재시작을 재현했습니다.
- 같은 개발 중 후보에서 생성된 HTML은 S4 브라우저 스모크에서 콘솔 오류 0건으로 통과했습니다.
- 저장소의 재현 가능한 브라우저 게임 E2E는 스크립트된 OpenAI 호환 모델을 실제 RafikX 파이프라인에 연결합니다. 에이전트가 HTML·CSS와 런타임 결함이 있는 Canvas 게임을 먼저 만들면 품질 게이트가 거부하고, 실패 증거를 받은 에이전트가 같은 턴에서 상태 머신을 수리한 뒤 Node와 Chrome으로 플레이·일시정지·재개·패배·재시작을 통과시킵니다. 이어 신선한 읽기 전용 검증자가 실제 HTML·JavaScript를 읽고 5개 관점을 모두 통과시킨 다음, 자동 수정 없는 최종 Node·Chrome 검증이 같은 산출물을 다시 통과해야 종료됩니다. 검증 계약이 모델 요청에 실제 포함되는지, 검증 도구가 없는 환경에서 통과로 가장하지 않는지도 고정합니다.
- 개발 중 후보 `040e2aa` 바이너리로 같은 게임에 단일 주석을 추가하는 후속 개발 요청을 실행해 12회 반복 후 정상 완료하고 `node --check`까지 통과했습니다.
- 후속 후보 `318b3b7`에서는 기존 게임의 복사본을 대상으로 실제 `rafikx ask`를 다시 실행했습니다. 접근 가능한 Pause 버튼을 25회 반복 안에 구현했고, 중간 패치·stale anchor 오류를 무변경으로 회복했습니다. 실제 Chrome에서 포인터 일시정지, Space 키 재개, 정지 중 게임 상태 고정, 재개 시 상태 보존, 접근성 트리의 button 역할·이름·pressed 상태를 확인한 뒤 `node --check`와 S4 콘솔 오류 0건으로 완료했습니다.
- 보안 후보 `b6b2244`의 공격성 페이지 재현에서는 `.env`, `.git/config`, `secret.js` 요청이 모두 404로 차단됐고 비밀값이 리포트에 노출되지 않았습니다. 정상 Canvas 게임은 같은 격리 게이트를 통과했습니다.
- 검증 후보 `d9ff15b`의 CLI 공격 재현에서는 무관한 루트 페이지 뒤의 중첩 앱, 비정형 `launch.html`, 주석의 apostrophe와 화살표 함수 정규식 뒤의 전이 import, 대소문자 변형 경로 뒤의 모듈 오류를 정확한 엔트리 경로와 `ReferenceError`로 실패 처리했습니다. 정상 엔트리 하나가 실행되지 않은 변경 JS를 가리는 경우도 해당 파일을 명시해 실패합니다. Node가 없는 단독 JavaScript와 Go 도구가 없는 워크스페이스는 필수 검증 부재로 실패했습니다. 단일·멀티라인·3줄·CRLF·장식·CSI·OSC `Authorization` 표식은 실패 출력 어디에도 남지 않았습니다.
- 회귀 테스트는 영문 개발 동작 분류, Bash 오류·결정적 변경 추적·원복·메타데이터 오탐·집계 상한·내부 링크 정체성·제외 디렉터리 내부 외부 링크 차단·비정상 UTF-8 절단, 혼합 경로 기준선, 실패한 하위 작업의 변경 전파, 분류 강제·공유 예산·미상 DAG 의존성·파일 감옥, 변경 관련 비정형/전이 브라우저 엔트리와 주석·화살표/제어문 정규식·양방향 대소문자 경로 탐색·미실행 변경 소스·프로젝트 상대 자산 로드, 브라우저 격리·항목/출력 상한·stderr 종료 시한·정확한 로컬 포트 경계·보안 헤더, Canvas clear·투명 paint/ImageData·불완전 path·텍스트 위장 거부와 Path2D·offscreen 도형 허용·trackerless 픽셀 거부, 자동 명령 출력 상한·이중 포크 후 재부모화된 세션 후손 종료·부모 marker FD 해제·3줄/CSI/OSC 분할 시크릿 정제·Codex 실제 토큰 상한과 정수 포화·프로세스 경계 ID·config 무덮어쓰기·동시 DB 초기화·v1.1.8 공개 API·불변 커밋 고정 updater, 읽기 전용 검증자 계약·수정 직후 기계 재검증·변경 감지·그룹 판정·닫힌 실패와 브라우저 게임 복구 계약을 고정합니다.

## 업그레이드

GitHub Release v1.1.9가 게시된 뒤 기존 터미널 설치는 `rafikx update`로 설치할 수 있습니다. Tauri 데스크탑 개선·검증·배포는 이번 CLI 릴리스에서 제외하고 다음 별도 단계에서 진행합니다.
