# RafikX v1.1.9 — 코딩 에이전트 복구

영문·한글 개발 요청과 재시도를 도구 가능한 개발 레인에 유지하고, 도구·검증·종료 상태를 실제 실행 증거로 판정하도록 하네스를 복구한 패치 릴리스입니다.

## 사건과 원인

- 영문 산출물 요청이 `Simple`로 분류되어 파일 도구 없이 종료됐고, `다시 해줘` 같은 후속 요청도 이전 개발 맥락을 잃었습니다.
- Bash의 비정상 종료가 성공 도구 결과로 전달되고, Bash가 만든 파일은 변경 증거에 포함되지 않았습니다.
- 목표 연속 실행과 검증 복구가 반복 예산을 각각 새로 받아 한 요청이 87~149회까지 늘어났습니다.
- 정상 게임도 짧은 중복 휴리스틱에 막혔으며, 미완료 단발 실행은 종료 코드 0을 반환했습니다.

## 수정

- **개발 요청 라우팅** — `create`, `write`, `fix`, `edit`, `add`, `remove`, `delete`, `rename`, `replace`, `upgrade` 등 영문 개발 동사와 앱·버튼·함수·모듈·페이지·테스트 같은 산출물 조합, HTML/CSS 확장자를 `Dev`로 분류합니다. 바로 앞 개발 턴의 재시도만 개발 레인을 상속하고, 사용자가 강제한 `simple` 분류는 자동 승격하지 않습니다.
- **도구 실행 계약** — Bash 비정상 종료를 오류로 전달하고, 명령 전후 워크스페이스의 결정적 전체 내용 해시 차이로 생성·수정·삭제·동일 크기 재작성·ignore 파일을 변경 증거에 기록합니다. 파일·항목·단일 파일·전체 해시 양·시간 상한을 두며, 추적할 수 없는 작업은 실행 전에 닫힌 실패로 중단합니다. 내용 해시에서 제외한 `.cache`, `.git`, `node_modules`, `target` 등의 내부도 경로 메타데이터는 순회해 워크스페이스 밖 링크를 Bash 실행 전에 차단합니다.
- **최종 변경 증거** — 처음 변경하기 전의 내용과 종료 시점의 내용을 비교합니다. 수정 후 원복은 변경으로 가장하지 않고, `touch`만 한 파일도 변경으로 오인하지 않으며, 성공·실패와 무관하게 하위 작업의 기준선을 부모 실행에 전파합니다. macOS의 `/tmp`와 `/private/tmp`처럼 심볼릭 링크로 표현이 달라지는 워크스페이스도 같은 물리 경로로 정규화하고, 워크스페이스 밖을 가리키는 링크는 Bash 실행 전에 차단합니다.
- **턴 단위 종료 예산** — 일반 연속 실행, 3~7개 그래프 노드와 재시도, 품질 복구, 독립 검증자가 최대 50회의 단일 예산을 공유합니다.
- **검증 복구** — HTML/JS 품질 실패를 같은 턴에서 수정·재검증합니다. 변경된 모든 HTML과 웹 소스 주변·중첩 `index.html`을 제한된 탐색으로 찾아 각각 실제 브라우저에서 엽니다. HTTP로 실제 모듈을 로드하고 `console.error`, 예외, Promise 거부, 리소스 404를 수집하며 브라우저 실패·미설치를 통과로 숨기지 않습니다.
- **검증 보안** — 브라우저에는 명시적인 웹 자산만 임시 스테이징하고 dotfile·자격 증명 파일을 제외합니다. 자산 수·파일·합계·시간과 브라우저 stderr·오류 수에 상한을 둡니다. stderr 상한 초과와 리더 종료 지연은 실패로 처리하고, 샌드박스 CSP와 현재 임시 서버 포트만 허용하는 오프라인 프록시로 외부 및 다른 로컬 서비스 통신을 차단하며 모든 종료 경로에서 서버·프로필을 정리합니다. 자동 품질 명령은 셸 문자열 대신 프로그램·인자 배열로 실행하고, 설치된 감사 도구의 실행 실패·시간 초과를 실패로 처리하며, 실패 증거와 실시간 출력의 경로·자격 증명·URL 쿼리·URL 사용자 정보를 정제합니다.
- **상태 정직성** — 도구 호출 문법 누출의 개발 승격이 실패하거나 개발 파일 변경이 없으면 완료로 보고하지 않습니다. 차단된 검증은 실패로 남고 검증자 수정 뒤 기계 검증을 다시 실행합니다. 읽기 전용 검증자가 `PASS` 도중 파일을 바꾸거나 검증자 바인드·전송·재개·변경 추적이 실패해도 활성 목표를 닫힌 실패로 종결합니다. 단일 검증자의 5개 그룹 판정은 정확성·보안·성능·가독성·API 설계 중 하나라도 실패하거나 누락되면 실제 실패로 집계합니다. `rafikx ask`와 `rafikx agent`의 미완료·실패·거부·취소는 비정상 종료 코드로 반환합니다.
- **Codex 백엔드 호환성** — ChatGPT Codex 응답 백엔드가 거부하는 `max_output_tokens` 필드를 전송하지 않도록 수정했습니다. 하네스 내부의 50회 턴 예산과 종료 조건은 그대로 유지됩니다.

## 검증

- 기본 기능 전체 459개와 `--no-default-features` 402개 테스트가 통과했습니다. 환경 의존 LSP 실동작 테스트 3개는 두 구성에서 기존대로 ignore 상태입니다.
- 실제 `rafikx ask`로 패키지 없는 마리오 스타일 브라우저 게임 3개 파일을 생성했습니다. 생성 에이전트의 Chrome CDP 검증에서 좌우 이동·점프·적 밟기·피해·패배·승리와 양쪽 재시작을 통과했고, 별도 상호작용 점검에서도 이동·점프·적 밟기·피해·패배·재시작을 재현했습니다.
- 생성된 HTML은 S4 브라우저 스모크에서 콘솔 오류 0건으로 통과했습니다.
- 수정된 최신 바이너리로 같은 게임에 단일 주석을 추가하는 후속 개발 요청을 실행해 12회 반복 후 정상 완료하고 `node --check`까지 통과했습니다.
- 최종 릴리스 후보 바이너리에는 기존 게임의 복사본을 대상으로 실제 `rafikx ask`를 다시 실행했습니다. 접근 가능한 Pause 버튼을 25회 반복 안에 구현했고, 중간 패치·stale anchor 오류를 무변경으로 회복했습니다. 실제 Chrome에서 포인터 일시정지, Space 키 재개, 정지 중 게임 상태 고정, 재개 시 상태 보존, 접근성 트리의 button 역할·이름·pressed 상태를 확인한 뒤 `node --check`와 S4 콘솔 오류 0건으로 완료했습니다.
- 공격성 페이지 재현에서는 `.env`, `.git/config`, `secret.js` 요청이 모두 404로 차단됐고 비밀값이 리포트에 노출되지 않았습니다. 정상 Canvas 게임은 같은 격리 게이트를 통과했습니다.
- 회귀 테스트는 영문 개발 동작 분류, Bash 오류·결정적 변경 추적·원복·메타데이터 오탐·집계 상한·제외 디렉터리 내부 외부 링크 차단, 혼합 경로 기준선, 실패한 하위 작업의 변경 전파, 분류 강제·예산 상한·파일 감옥, 다중·중첩 브라우저 엔트리 탐색, 브라우저 격리·항목/출력 상한·stderr 종료 시한·정확한 로컬 포트 경계·보안 헤더, 검증자 변경 감지·그룹 판정·닫힌 실패와 중복 오탐 계약을 고정합니다.

## 업그레이드

GitHub Release v1.1.9가 게시된 뒤 기존 터미널 설치는 `rafikx update`로 설치할 수 있습니다. 데스크탑도 같은 `agent-harness` 코어와 v1.1.9 버전을 사용합니다.
