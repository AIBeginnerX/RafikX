# RafikX v1.1.9 — 코딩 에이전트 복구

영문·한글 개발 요청과 재시도를 도구 가능한 개발 레인에 유지하고, 도구·검증·종료 상태를 실제 실행 증거로 판정하도록 하네스를 복구한 패치 릴리스입니다.

## 사건과 원인

- 영문 산출물 요청이 `Simple`로 분류되어 파일 도구 없이 종료됐고, `다시 해줘` 같은 후속 요청도 이전 개발 맥락을 잃었습니다.
- Bash의 비정상 종료가 성공 도구 결과로 전달되고, Bash가 만든 파일은 변경 증거에 포함되지 않았습니다.
- 목표 연속 실행과 검증 복구가 반복 예산을 각각 새로 받아 한 요청이 87~149회까지 늘어났습니다.
- 정상 게임도 짧은 중복 휴리스틱에 막혔으며, 미완료 단발 실행은 종료 코드 0을 반환했습니다.

## 수정

- **개발 요청 라우팅** — `create`, `write`, `fix`, `edit` 등 영문 개발 동사와 산출물 조합, HTML/CSS 확장자를 `Dev`로 분류합니다. 바로 앞 개발 턴의 재시도만 개발 레인을 상속하고, 사용자가 강제한 `simple` 분류는 자동 승격하지 않습니다.
- **도구 실행 계약** — Bash 비정상 종료를 오류로 전달하고, 명령 전후 워크스페이스의 경량 내용 해시 차이로 생성·수정·삭제·동일 크기 재작성·ignore 파일을 변경 증거에 기록합니다. 대형 파일은 제한된 구간만 읽고 의존성·캐시 폴더는 제외합니다.
- **턴 단위 종료 예산** — 일반 연속 실행, 3~7개 그래프 노드와 재시도, 품질 복구, 독립 검증자가 최대 50회의 단일 예산을 공유합니다.
- **검증 복구** — HTML/JS 품질 실패를 같은 턴에서 수정·재검증합니다. HTTP로 실제 모듈을 로드하고 `console.error`, 예외, Promise 거부, 리소스 404를 수집하며 브라우저 실패·미설치를 통과로 숨기지 않습니다.
- **검증 보안** — 브라우저에는 명시적인 웹 자산만 임시 스테이징하고 dotfile·자격 증명 파일을 제외합니다. CSP와 오프라인 프록시로 외부 통신을 차단하며 모든 종료 경로에서 서버·프로필을 정리합니다. 자동 품질 명령은 셸 문자열 대신 프로그램·인자 배열로 실행하고, 실패 증거의 경로·자격 증명·URL 쿼리를 정제합니다.
- **상태 정직성** — 도구 호출 문법 누출의 개발 승격이 실패하거나 개발 파일 변경이 없으면 완료로 보고하지 않습니다. 차단된 검증은 실패로 남고 검증자 수정 뒤 기계 검증을 다시 실행하며, 검증 자체 오류도 활성 목표를 실패로 종결합니다. `rafikx ask`와 `rafikx agent`의 미완료·실패·거부·취소는 비정상 종료 코드로 반환합니다.

## 검증

- 기본 기능 전체 438개와 `--no-default-features` 381개 테스트가 통과했습니다. 환경 의존 LSP 실동작 테스트 3개는 두 구성에서 기존대로 ignore 상태입니다.
- 실제 `rafikx ask`로 마리오 스타일 브라우저 게임을 생성하고, 시작·좌우 이동·점프·적 충돌·패배 재시작·승리·승리 재시작·R 키 재시작을 포함한 Playwright 체크 20개를 통과했습니다.
- 생성된 HTML은 S4 브라우저 스모크에서 콘솔 오류 0건으로 통과했습니다.
- 공격성 페이지 재현에서는 `.env`, `.git/config`, `secret.js` 요청이 모두 404로 차단됐고 비밀값이 리포트에 노출되지 않았습니다. 정상 Canvas 게임은 같은 격리 게이트를 통과했습니다.
- 회귀 테스트는 영문 게임 분류, Bash 오류·변경 추적, 분류 강제·예산 상한·파일 감옥, 브라우저 격리·보안 헤더, 중복 오탐 계약을 고정합니다.

## 업그레이드

GitHub Release v1.1.9가 게시된 뒤 기존 터미널 설치는 `rafikx update`로 설치할 수 있습니다. 데스크탑도 같은 `agent-harness` 코어와 v1.1.9 버전을 사용합니다.
