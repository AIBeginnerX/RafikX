# RafikX v1.1.9 — 코딩 에이전트 복구

영문·한글 개발 요청과 재시도를 도구 가능한 개발 레인에 유지하고, 도구·검증·종료 상태를 실제 실행 증거로 판정하도록 하네스를 복구한 패치 릴리스입니다.

## 사건과 원인

- 영문 산출물 요청이 `Simple`로 분류되어 파일 도구 없이 종료됐고, `다시 해줘` 같은 후속 요청도 이전 개발 맥락을 잃었습니다.
- Bash의 비정상 종료가 성공 도구 결과로 전달되고, Bash가 만든 파일은 변경 증거에 포함되지 않았습니다.
- 목표 연속 실행과 검증 복구가 반복 예산을 각각 새로 받아 한 요청이 87~149회까지 늘어났습니다.
- 정상 게임도 짧은 중복 휴리스틱에 막혔으며, 미완료 단발 실행은 종료 코드 0을 반환했습니다.

## 수정

- **개발 요청 라우팅** — 영문 개발 동사와 산출물 조합, HTML/CSS 확장자를 `Dev`로 분류하고 개발 도구 이력이 있는 재시도는 개발 레인을 상속합니다.
- **도구 실행 계약** — Bash 비정상 종료를 오류로 전달하고, 명령 전후 워크스페이스 차이로 생성·수정·삭제 파일을 변경 증거에 기록합니다.
- **턴 단위 종료 예산** — 본 작업·연속 실행·검증 복구가 최대 50회의 단일 예산을 공유합니다. 원 요청의 완료 기준 통과 뒤에는 보너스 목표를 만들지 않고 종료합니다.
- **검증 복구** — HTML/JS 품질 실패를 같은 턴에서 수정·재검증하고, 브라우저 실패·미설치를 통과로 숨기지 않습니다. 의미 있는 중복은 S7 권고로 남기되 산출물을 차단하지 않습니다.
- **상태 정직성** — 검증 전 목표는 완료로 저장하지 않으며, 실패한 모델 선택은 다음 턴에 고정하지 않습니다. `rafikx ask`와 `rafikx agent`의 미완료·실패·거부·취소는 비정상 종료 코드로 반환합니다.

## 검증

- 기본 기능 전체 429개와 `--no-default-features` 372개 테스트가 통과했습니다. 환경 의존 LSP 실동작 테스트 3개는 두 구성에서 기존대로 ignore 상태입니다.
- 실제 `rafikx ask`로 마리오 스타일 브라우저 게임을 생성하고, 시작·좌우 이동·점프·적 충돌·패배 재시작·승리·승리 재시작·R 키 재시작을 포함한 Playwright 체크 20개를 통과했습니다.
- 생성된 HTML은 S4 브라우저 스모크에서 콘솔 오류 0건으로 통과했습니다.
- 회귀 테스트는 영문 게임 분류, Bash 오류·변경 추적, 중복 오탐·권고 계약을 고정합니다.

## 업그레이드

기존 터미널 설치는 `rafikx update`로 최신 릴리스를 설치할 수 있습니다. 데스크탑도 같은 `agent-harness` 코어와 v1.1.9 버전을 사용합니다.
