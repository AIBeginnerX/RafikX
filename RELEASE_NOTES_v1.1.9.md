# RafikX CLI v1.1.9 — 코딩 에이전트 복구

영문·한글 개발 요청과 재시도를 도구 가능한 개발 레인에 유지하고, 도구·검증·종료 상태를 실제 실행 증거로 판정하도록 하네스를 복구한 패치 릴리스입니다.

## 사건과 원인

- 영문 산출물 요청이 `Simple`로 분류되어 파일 도구 없이 종료됐고, `다시 해줘` 같은 후속 요청도 이전 개발 맥락을 잃었습니다.
- Bash의 비정상 종료가 성공 도구 결과로 전달되고, Bash가 만든 파일은 변경 증거에 포함되지 않았습니다.
- 목표 연속 실행과 검증 복구가 반복 예산을 각각 새로 받아 한 요청이 87~149회까지 늘어났습니다.
- 병렬 위임의 하위 에이전트가 각자 반복 예산을 새로 받았고, 실패·제한·취소된 하위 작업도 성공 도구 결과처럼 부모에게 전달되어 부모가 정상 완료로 끝날 수 있었습니다.
- 정상 게임도 짧은 중복 휴리스틱에 막혔으며, 미완료 단발 실행은 종료 코드 0을 반환했습니다.

## 수정

- **개발 요청 라우팅** — `create`, `write`, `fix`, `edit`, `add`, `remove`, `delete`, `rename`, `replace`, `upgrade` 등 영문 개발 동사와 앱·버튼·함수·모듈·페이지·테스트 같은 산출물 조합, HTML/CSS 확장자를 `Dev`로 분류합니다. 바로 앞 개발 턴의 재시도만 개발 레인을 상속하고, 사용자가 강제한 `simple` 분류는 자동 승격하지 않습니다.
- **도구 실행 계약** — Bash 비정상 종료를 오류로 전달하고, 명령 전후 워크스페이스의 결정적 전체 내용 해시 차이로 생성·수정·삭제·동일 크기 재작성·ignore 파일을 변경 증거에 기록합니다. 파일·항목·단일 파일·전체 해시 양·시간 상한을 두며, 추적할 수 없는 작업은 실행 전에 닫힌 실패로 중단합니다. 내용 해시에서 제외한 `.cache`, `.git`, `node_modules`, `target` 등의 내부도 경로 메타데이터는 순회해 워크스페이스 밖 링크를 Bash 실행 전에 차단합니다. stdout·stderr는 캡처 상한 뒤에도 EOF까지 동시에 비워 유한한 대량 출력이 파이프에서 막히지 않으며, 보관분만 20KB로 자릅니다. 모든 도구의 성공 출력·오류·승인 미리보기·검증 명령과 최종 검증 이력은 실시간 UI, 실행 로그, 콜백, 상위 모델의 도구 결과와 목표 저장 상태에 들어가기 전에 ANSI·OSC·자격 증명·로컬 경로를 정제하고 길이를 제한합니다.
- **최종 변경 증거** — 처음 변경하기 전의 내용과 종료 시점의 내용을 비교합니다. 수정 후 원복은 변경으로 가장하지 않고, `touch`만 한 파일도 변경으로 오인하지 않으며, 성공·실패와 무관하게 하위 작업의 기준선을 부모 실행에 전파합니다. macOS의 `/tmp`와 `/private/tmp`처럼 심볼릭 링크로 표현이 달라지는 워크스페이스도 같은 물리 경로로 정규화하되, 최종 심볼릭 링크 자체는 대상 파일로 치환하지 않고 변경 증거의 경로 정체성으로 보존합니다. 내부 심볼릭 링크의 생성·삭제·대상 변경도 별도 증거로 기록하고, 워크스페이스 밖을 가리키는 링크는 Bash 실행 전에 차단합니다.
- **턴 단위 종료 예산과 순서** — 계획·일반 연속 실행·병렬 하위 에이전트·3~7개 그래프 노드와 재시도·품질 복구·독립 검증자가 원자적으로 청구하는 최대 50회의 단일 예산을 실행 트리 전체에서 공유합니다. 병렬 위임도 이 상한을 넘을 수 없고 최종 반복 수는 루트와 모든 하위 실행의 실제 합계입니다. 그래프가 존재하지 않는 선행 노드를 참조하면 간선을 조용히 버리지 않고 계획 전체를 기본 파이프라인으로 폴백합니다.
- **위임 실패 정직성** — 하위 작업은 `ok`일 때만 성공 도구 결과가 됩니다. 실패·미완료·거부·취소·제한과 알 수 없는 상태는 정제·상한 처리된 오류로 부모에게 전달하고, 부모가 같은 입력의 위임을 실제 성공으로 재시도하기 전까지 미해결 상태를 유지하므로 텍스트만으로 정상 완료를 선언할 수 없습니다. 직접 provider 오류도 lifecycle 저장·실시간 발행·부모 도구 결과보다 먼저 비밀값을 제거하고 길이를 제한합니다. 실패한 하위 작업이 만든 파일 변경 증거는 오류와 별개로 계속 부모에게 전파됩니다.
- **검증 복구** — 명시적 검증 명령이 성공해도 변경 파일 전체의 품질 게이트를 반드시 이어서 실행하며, Rust와 JavaScript가 함께 바뀌면 Rust가 주 언어여도 각 JavaScript 파일을 Node로 검사합니다. 혼합 Rust·Python·Go 변경도 각 비주력 언어의 구문 검사를 생략하지 않습니다. HTML/JS 품질 실패를 같은 턴에서 수정·재검증합니다. 기본 rafikx 엔진은 개발 작업에서 신선한 읽기 전용 검증자를 거치며, 검증자 통과 뒤에는 자동 수정을 허용하지 않는 최종 기계 검증을 실행해 리뷰 이후 파일이 바뀐 채 성공하는 순서 역전을 막습니다. 변경된 모든 HTML과 변경 웹 소스의 상위 엔트리, 비정형 이름의 HTML, 실제 파일 참조, 로컬 모듈의 전이 import, 같은 패키지의 대표 엔트리를 제한된 탐색으로 찾아 각각 실제 브라우저에서 엽니다. 브라우저 게임 작업은 같은 공유 스크립트를 참조하는 모든 HTML 엔트리를 끝까지 탐색하므로 먼저 찾은 `index.html`이 다른 게임 페이지를 가리지 못합니다. 상위 디렉터리의 무관한 `index.html`은 변경 소스를 실제 참조할 때만 진입점으로 인정하므로 중첩 앱의 런타임 오류를 가리지 못합니다. 변경된 HTML/CSS를 실행할 엔트리가 없거나 참조 그래프를 제한 안에서 증명할 수 없으면 닫힌 실패로 처리합니다. `public/index.html`과 `src/app.js`처럼 엔트리 바깥의 프로젝트 자산도 상대 경로를 보존해 스테이징합니다. HTTP로 실제 모듈을 로드하고 `console.error`, 예외, Promise 거부, 리소스 404를 수집하며 브라우저 실패·미설치를 통과로 숨기지 않습니다. Snake·Tetris·Pong·Breakout·일반 HTML 게임과 playable·interactive mode 요청을 포함한 브라우저 게임 의도는 작업 문장에서 공통 분류해 검증 계약을 강제하며, 게임과 차트를 함께 요청해도 게임 계약을 유지합니다. Canvas뿐 아니라 SVG·DOM 게임도 대상이며, 단일·다중 엔트리를 모두 상태 전이 계약으로 검사하므로 모델이 meta/API·대시보드·미끼 페이지로 검증을 우회할 수 없습니다. 실제 키 입력으로 `ready → playing → paused → playing → lost → ready`와 재시작 횟수를 검사하고, 각 상태에서 보이는 게임 surface의 DOM·Canvas 지문도 함께 달라져야 하므로 화면과 분리된 가짜 상태 API는 실패합니다. `lost`가 아닌 상태의 `KeyR`이 상태나 횟수를 바꿔도 실패합니다.
- **검증 보안** — `--yes`도 Bash 안전 차단을 우회하지 못하며, 분리·결합 재귀 플래그, `--`, 따옴표, 중첩 셸과 Unix 홈·루트 및 Windows `%USERPROFILE%`·드라이브 루트 삭제를 정규화해 차단합니다. 브라우저에는 선택된 HTML에서 실제로 도달 가능한 `src`·`href`·CSS `url()`·모듈 import·`fetch()`·스프라이트 `.src` 자산만 전이 그래프로 임시 스테이징하고, dotfile·자격 증명 파일과 워크스페이스 밖 링크를 제외합니다. 민감한 자산을 명시적으로 참조해도 닫힌 실패로 중단합니다. 자산 수·파일·합계·시간과 브라우저 stderr·오류 수에 상한을 둡니다. stderr 상한 초과와 리더 종료 지연은 실패로 처리하고, 샌드박스 CSP와 현재 임시 서버 포트만 허용하는 오프라인 프록시로 외부 및 다른 로컬 서비스 통신을 차단하며 모든 종료 경로에서 서버·프로필을 정리합니다. 자동 품질 명령과 의존성 감사는 셸 문자열 대신 프로그램·인자 배열로 실행하고, stdout·stderr를 고정 상한으로 수집합니다. Windows는 명령을 정지 상태로 시작해 kill-on-close Job Object에 넣은 다음 재개하므로 breakaway를 허용하지 않은 전체 자손을 커널 경계로 정리합니다. Unix는 프로세스 그룹·부모관계·실행별 환경 표식·비공개 상속 FD를 교차 확인하고, 조회가 실패해도 그룹·루트·이미 찾은 PID의 종료와 비동기 회수 확인을 끝까지 시도한 뒤 오류를 반환합니다. SIGKILL 전달 성공은 커널 전달 영수증으로 판정해 macOS의 지연된 프로세스 목록 때문에 정상 종료를 실패로 뒤집지 않되, 전달 자체가 실패한 현재 PID는 오류로 남깁니다. 이 Unix 경로는 일반 코딩 명령의 누수를 줄이는 운영 안전장치이며, 환경과 모든 상속 FD를 의도적으로 지운 뒤 daemonize하는 적대 프로세스를 가두는 보안 샌드박스는 아닙니다. Rust 포맷에는 Rust 변경 파일만 전달하고 ANSI 색상을 제거한 Clippy 결과를 변경 파일에 귀속해 기존 저장소 부채와 새 회귀를 구분합니다. `cfg(test)` 모듈과 의미상 테스트 디렉터리·파일만 제외하며 `contest.rs`, `latest.rs`, `example_client.rs` 같은 운영 파일은 계속 스캔합니다. 주석 안의 가짜 `#[cfg(test)] mod`는 운영 코드 마스크를 바꾸지 못하고, 바로 인접한 `SAFETY` 근거 없는 `unsafe` 토큰은 주석으로 공백을 가려도 실패시킵니다. 필수 도구가 없으면 닫힌 실패로 처리합니다. 패키지 없는 순수 JavaScript도 Node 구문 검사와 실제 브라우저 게이트가 모두 있어야 통과하며, Node가 없거나 변경된 HTML/CSS의 엔트리가 없으면 실패합니다. 실패 증거와 실시간 출력의 명령·메모·파일 경로를 필드별·전체 길이 상한으로 제한하고, 로컬 경로·공백·줄바꿈 포함 대입형 자격 증명·ANSI로 분할된 민감 키·장식형 인증 헤더·URL 쿼리·URL 사용자 정보·URL 경로의 Telegram 봇 토큰·AWS 액세스 키를 정제하며, 비정상 UTF-8 Bash 출력도 문자 경계에서 안전하게 자릅니다.
- **상태 정직성** — 도구 호출 문법 누출의 개발 승격이 실패하거나 개발 파일 변경이 없으면 완료로 보고하지 않습니다. 차단되거나 거부된 검증은 성공으로 사라지지 않고, 그래프 노드의 재시도 결과도 다음 노드로 넘기기 전에 다시 검증합니다. Cargo·Python 자동 명령이 없는 HTML/JavaScript 노드도 구문·브라우저·보안 경계를 통과해야 하며, 경계 검증이 두 번 실패한 노드는 에이전트 본문 상태가 `ok`여도 그래프 전체를 `fail`로 끝냅니다. 검증자 수정 뒤에는 기계 검증을 다시 실행합니다. 빌드·테스트·품질 게이트는 상위 하네스가 실행하고, 읽기 전용 검증자는 `read_file`, `list_dir`, `grep`, `glob` 네 도구를 누락·중복 없이 정확히 받아 그 시스템 상태와 직접 읽은 코드·계약을 대조합니다. 이름 확장으로 `todo_write` 같은 변경 도구가 섞일 수 없고, 실행을 요구하면서 Bash를 주지 않던 모순도 없습니다. 검증자가 `PASS` 도중 파일을 바꾸거나 검증자 바인드·전송·재개·변경 추적이 실패해도 활성 목표를 닫힌 실패로 종결합니다. 단일 검증자의 5개 그룹 판정은 정확성·보안·성능·가독성·API 설계 중 하나라도 실패하거나 누락되면 실제 실패로 집계합니다. `rafikx ask`와 `rafikx agent`의 미완료·실패·거부·취소는 비정상 종료 코드로 반환합니다.
- **Codex 백엔드 호환성과 비용 가드** — ChatGPT Codex 응답 백엔드가 거부하는 `max_output_tokens` 필드는 전송하지 않되, 스트림·비스트림 응답 모두에서 서버가 보고한 실제 출력 토큰을 설정된 `max_tokens`와 대조합니다. 사용량 보고가 없거나 상한을 넘긴 응답의 도구 호출은 실행하지 않고 미완료로 되먹이며, 텍스트 바이트 수를 토큰으로 오판하지 않습니다. 하네스 내부의 50회 턴 예산과 종료 조건도 그대로 유지됩니다.
- **동시 첫 실행 설정** — 여러 터미널 프로세스가 같은 빈 데이터 폴더에서 동시에 시작해도 완성된 기본 config를 같은 폴더의 임시 파일에 기록한 뒤, 기존 파일을 교체할 수 없는 원자적 hard-link 게시로 최초 파일만 채택합니다. 사용자가 동시에 저장한 config는 보존하고 병렬 테스트도 각각 고유한 설정 폴더를 사용합니다.
- **동시 실행 데이터베이스** — 여러 터미널 세션 또는 병렬 테스트가 같은 SQLite 파일을 동시에 열어도 WAL 전환의 일시 잠금에 안전하게 양보하고, 마이그레이션 전체를 즉시 쓰기 트랜잭션 하나로 직렬화해 중복 버전 삽입과 부분 적용을 막습니다.
- **크로스플랫폼 업데이터** — `sh` 스크립트 의존성을 제거하고 Rust에서 정확한 `refs/tags/vX.Y.Z`만 fetch·detached checkout한 뒤 HEAD와 태그 커밋을 다시 대조합니다. 비공개 저장소용 GitHub 자격 증명 옵션은 Git 하위 명령 앞에 배치하고 이 fetch 한 번에만 환경으로 전달하며, checkout·검증·빌드에는 제거합니다. Unix는 검증된 소스에서 바로 설치하고, Windows는 별도 루트에 빌드한 실행 파일을 현재 프로세스 종료 후 PowerShell로 교체·정리합니다.

## 검증

- 기본 기능 전체 532개와 `--no-default-features` 475개 테스트가 통과했습니다. 환경 의존 LSP 실동작 테스트 3개와 릴리스용 브라우저 게임 E2E 1개는 기본 실행에서 ignore 상태이며, 브라우저 게임 E2E는 이번 릴리스 게이트에서 Node와 Chrome을 사용해 별도로 통과했습니다. 실제 CLI 혼합 언어 게이트는 깨진 JavaScript를 `S3-syntax`에서 거부하고 수정 뒤 Rust·Node·감사·전체 테스트를 함께 통과했습니다. 이번 릴리스 단계는 CLI만 검증했으며 Tauri 데스크탑은 별도 후속 단계로 보류했습니다.
- 실제 `rafikx ask`로 패키지 없는 마리오 스타일 브라우저 게임 3개 파일을 생성했습니다. 생성 에이전트의 Chrome CDP 검증에서 좌우 이동·점프·적 밟기·피해·패배·승리와 양쪽 재시작을 통과했고, 별도 상호작용 점검에서도 이동·점프·적 밟기·피해·패배·재시작을 재현했습니다.
- 생성된 HTML은 S4 브라우저 스모크에서 콘솔 오류 0건으로 통과했습니다.
- 저장소의 재현 가능한 브라우저 게임 E2E는 스크립트된 OpenAI 호환 모델을 실제 RafikX 파이프라인에 연결합니다. 에이전트가 HTML·CSS와 런타임 결함이 있는 Canvas 게임을 먼저 만들면 품질 게이트가 거부하고, 실패 증거를 받은 에이전트가 같은 턴에서 상태 머신을 수리한 뒤 Node와 Chrome으로 플레이·일시정지·재개·패배·재시작을 통과시킵니다. 이어 신선한 읽기 전용 검증자가 실제 HTML·JavaScript를 읽고 5개 관점을 모두 통과시킨 다음, 자동 수정 없는 최종 Node·Chrome 검증이 같은 산출물을 다시 통과해야 종료됩니다. 검증 계약이 모델 요청에 실제 포함되는지, 검증 도구가 없는 환경에서 통과로 가장하지 않는지도 고정합니다.
- 수정된 최신 바이너리로 같은 게임에 단일 주석을 추가하는 후속 개발 요청을 실행해 12회 반복 후 정상 완료하고 `node --check`까지 통과했습니다.
- 최종 릴리스 후보 바이너리에는 기존 게임의 복사본을 대상으로 실제 `rafikx ask`를 다시 실행했습니다. 접근 가능한 Pause 버튼을 25회 반복 안에 구현했고, 중간 패치·stale anchor 오류를 무변경으로 회복했습니다. 실제 Chrome에서 포인터 일시정지, Space 키 재개, 정지 중 게임 상태 고정, 재개 시 상태 보존, 접근성 트리의 button 역할·이름·pressed 상태를 확인한 뒤 `node --check`와 S4 콘솔 오류 0건으로 완료했습니다.
- 공격성 페이지 재현에서는 `.env`, `.git/config`, `secret.js` 요청이 모두 404로 차단됐고 비밀값이 리포트에 노출되지 않았습니다. 정상 Canvas 게임은 같은 격리 게이트를 통과했습니다.
- 실제 후보 CLI 공격 재현에서 무관한 루트 페이지 뒤의 중첩 앱, 비정형 `launch.html`, 주석의 apostrophe와 화살표 함수 정규식 뒤의 전이 import, 대소문자 변형 경로 뒤의 모듈 오류를 정확한 엔트리 경로와 `ReferenceError`로 실패 처리했습니다. 정상 엔트리 하나가 실행되지 않은 변경 JS를 가리는 경우도 해당 파일을 명시해 실패합니다. Node가 없는 단독 JavaScript와 Go 도구가 없는 워크스페이스는 필수 검증 부재로 실패했습니다. 단일·멀티라인·3줄·CRLF·장식·CSI·OSC `Authorization` 표식은 실패 출력 어디에도 남지 않았습니다.
- 회귀 테스트는 영문 개발 동작 분류, Bash 오류·결정적 변경 추적·원복·메타데이터 오탐·집계 상한·내부 링크 정체성·제외 디렉터리 내부 외부 링크 차단·비정상 UTF-8 절단, 혼합 경로 기준선, 실패한 하위 작업의 변경 전파, 분류 강제·공유 예산·미상 DAG 의존성·파일 감옥, 변경 관련 비정형/전이 브라우저 엔트리와 주석·화살표/제어문 정규식·양방향 대소문자 경로 탐색·미실행 변경 소스·프로젝트 상대 자산 로드, 브라우저 격리·항목/출력 상한·stderr 종료 시한·정확한 로컬 포트 경계·보안 헤더, 자동 명령 출력 상한·이중 포크 후 재부모화된 세션 후손 종료·3줄/CSI/OSC 분할 시크릿 정제·Codex 실제 토큰 상한과 정수 포화·config 무덮어쓰기·동시 DB 초기화·v1.1.8 공개 API·업데이터 fetch 전용 자격 증명과 argv 순서, 읽기 전용 검증자 계약·수정 직후 기계 재검증·변경 감지·그룹 판정·닫힌 실패와 브라우저 게임 복구 계약을 고정합니다.

## 업그레이드

GitHub Release v1.1.9가 게시된 뒤 기존 터미널 설치는 `rafikx update`로 설치할 수 있습니다. Tauri 데스크탑 개선·검증·배포는 이번 CLI 릴리스에서 제외하고 다음 별도 단계에서 진행합니다.
